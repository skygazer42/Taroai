# Design Implementation Plan: OperationsDrawer

## Summary
- **Scope:** 组件(Operations 抽屉,Run / Inspect / Admin 三视图)
- **Target:** `apps/web/index.html`(operations-drawer,约 309–903 行)+ `apps/web/assets/styles.css` + `apps/web/assets/main.js`
- **胜出变体:** B(统一时间线)+ 综合 A/C/D/E 的元素
- **关键改进:**
  - 用单条时间线替代 Run 视图的 11 个并列面板(Execution / Evidence / Timeline 三处重复信息合并)
  - 粘性状态栏:运行 ID + 阶段 chip + 渐变进度条 + 待审批快捷入口(点击滚动定位)
  - 审批作为时间线事件内联处理(Approve / Reject 就地操作)
  - 终端输出、产物、计划等证据以 chip 形式挂在事件上,点击内联展开
  - Inspect 视图紧凑化(2 列 key-value 网格,等宽数字)
  - Admin 视图收纳化(Solution Packs / Skills / Customer Success 三个手风琴分组,CS 的 4 个子流程拆为按需进入的子工作台)
  - 所有 "--" / "Waiting" 空状态替换为解释性文案(如「本次运行未使用浏览器;使用 Browser use 的运行会在这里显示会话与截图」)
  - 「取消运行」降级为底部幽灵按钮(红色文字),不再与 Retry 并列置顶

## 顺手修复(本次审查发现的确定 Bug)
- [x] `index.html:494/502/721/747/779`:sprite 引用 `#refresh-cw` → 改为 `#lucide-refresh-cw`(当前刷新图标全部显示为空白)
- [x] `index.html:352/490/571`:三个重复的 `data-workbench-view="run"` 容器合并为一个
- [x] 统一 sprite 路径写法(`/assets/`、`./assets/`、`assets/` 混用 → 统一为 `./assets/`)
- [x] CSS / JS 缓存版本号同步(统一为 `20260726-ops1`)
- [x] `<html lang>` 随 i18n 切换动态更新(确认 i18n.js:8 已动态设置,setLocale 刷新页面时生效,无需改动)

## Files to Change
- [x] `apps/web/index.html` — 重写 operations-drawer 区块:粘性状态栏 + 时间线容器 + Admin 手风琴;保留全部 `data-*` 钩子与 `data-testid`
- [x] `apps/web/assets/styles.css` — 新增状态栏、进度条、手风琴、run-actions 样式(文件末尾 `.ops-*` 区块,复用现有 token,含 reduced-motion 处理)
- [x] `apps/web/assets/main.js` — `renderOpsStatusbar()`:进度条 / live dot / 审批 chip;chip 点击 `revealApprovalPanel()` 滚动定位 + 高亮。注:保留了现有分面板渲染(收纳进手风琴),未做完整时间线条目重写
- [x] `apps/web/tests/` — 现有测试(account-identity / files-ui)不依赖 drawer 结构,直接运行全部通过

## Implementation Steps
1. **样式移植**:把预览稿的 `.f-*` 样式(状态栏、时间线、chip、手风琴、紧凑网格、term-tail)并入 `styles.css`,改用项目命名习惯(如 `.ops-timeline`、`.ops-statusbar`)
2. **HTML 结构替换**:operations-drawer 内改为 `状态栏 + 三个 data-workbench-view 容器`;Run 视图放 `<ol class="ops-timeline" data-timeline-list>`;保留 `data-run-control-status`、`data-execution-*`、`data-evidence-*` 等钩子(可挂在状态栏与时间线条目上)
3. **JS 渲染改造**(`main.js`):
   - 后端事件流(run created / plan / approval / sandbox / artifact / browser)→ 映射为时间线条目类型(icon、文案、chips、状态 class:`live` / `warn` / `ghost`)
   - 进度条宽度 = 已完成阶段 / 5;状态 chip 文案 = 当前阶段 + 步数
   - 待审批时显示状态栏琥珀 chip,`scrollIntoView` 定位 + 短暂高亮
   - 证据对象(terminal / artifact storage object)渲染为事件 chip,点击内联展开面板
4. **Inspect 视图**:trace / runtime-state 数据填入紧凑网格(`data-trace-*`、`data-runtime-*` 钩子保留)
5. **Admin 视图**:三个手风琴分组;Customer Success 现有表单按「健康度 / 技能请求 / 候选评审 / 发布草稿」拆为四个子面板,默认只显示入口按钮
6. **空状态文案**:集中定义在 `i18n.js`(中英双语),替换所有 "--" / "Waiting" / "No …" 占位
7. **回归**:跑 `apps/web/tests/`,人工核对 artifact-open / operations-open 两种 sidecar 模式下的显隐逻辑(styles.css 8300 行附近有针对 workbench 的 `display:none !important` 级联,需同步调整)

## Component API(渲染层约定)
- **时间线条目**:`{ type, time, title, subtitle, state: 'done'|'live'|'warn'|'ghost', chips: [{label, objectId, expandable}], actions: [{label, kind}] }`
- **状态栏**:`{ runId, stage, stepIndex, stepTotal, progress, pendingApprovals }`
- **事件**:`data-thread-*` / 审批 approve/reject 回调沿用现有 `#approve-button` / `#reject-button` 逻辑,迁移到内联按钮

## Required UI States
- **无运行(空)**:时间线区域显示单条引导文案「开始一次运行后,事件会实时显示在这里」+ 最近运行入口
- **进行中**:live 节点脉冲(尊重 `prefers-reduced-motion`),进度条渐变推进
- **待审批**:状态栏琥珀 chip + 时间线 warn 节点自动滚动可达
- **失败**:红色节点 + 错误分类文案 + Retry 按钮内联在失败事件上
- **完成**:进度条满格转绿,底部显示产物汇总条目

## Accessibility Checklist
- [x] Tab 切换用 `role="tablist"` + `aria-selected` + `aria-controls` / `role="tabpanel"`,方向键(←→↑↓ Home End)+ roving tabindex 可切换
- [x] 内联展开使用原生 `<details>/<summary>`(自带展开语义,无需 `aria-expanded`)
- [x] 进度条 `role="progressbar"` + `aria-valuenow`(renderOpsStatusbar 同步)
- [x] 审批快捷入口与内联按钮均可键盘触达,焦点环沿用全局 `:focus-visible`
- [x] 时间线用 `<ol>` 保持语义顺序;状态栏 `aria-live="polite"`

## Testing Checklist
- [x] 状态栏模型(进度计算 / dot 状态优先级)单元测试:`tests/ops-status.test.mjs`(5 个用例)
- [ ] 审批 approve/reject 内联操作的集成测试(需要后端,未做)
- [x] `data-testid` 选择器回归:main.js 全部选择器对照新 HTML 校验通过(唯一缺失的 `data-plan-pill` 为 HEAD 已有的历史遗留,有空值保护)
- [x] 浏览器冒烟(Playwright + 系统 Chrome):Run/Inspect/Admin 三视图截图、手风琴展开、方向键切换 Tab、状态栏空态,均正常;artifact-open 级联经代码审查确认结构未变(artifact-list 仍为 run 视图直接子元素)

## Design Tokens
- 全部复用现有 token:`--signal(-quiet/-strong)`、`--amber`、`--green`、`--red`、`--edge(-strong)`、`--paper(-raised/-inset)`、`--radius(-small)`、`--shadow-panel`
- 新增建议:`--ops-node-size: 21px`(时间线节点)、进度渐变 `linear-gradient(90deg, var(--green), var(--signal))`

---

*Generated by Design Lab (2026-07-26)*
