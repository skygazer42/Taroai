# Taroai Plans Index

These plans should be read and implemented in order.

Before expanding any plan that references LangGraph, LangChain, LlamaIndex, MCP, E2B, pgvector, Redis, PostgreSQL RLS, Next.js, OpenTelemetry, or other external terms, read `research-grounding.md` and verify the current code state. Planned adapter seams must not be described as implemented capabilities.

Review artifacts:

- `research-grounding.md`
  Official-source terminology notes and current-code grounding for framework, protocol, sandbox, storage, observability, and frontend terms.

- `review-readiness-audit.md`
  Requirement-by-requirement evidence map for plan coverage, current-code facts, open decisions, and review readiness.

- `completion-audit.md`
  Final audit that distinguishes completed plan-writing scope from open human review decisions.

- `mvp-review-packet.md`
  Short review packet for approving or revising the MVP cloud PoC scope.

- `review-status.md`
  Current status for each plan and review group.

- `review-decisions.md`
  Accepted/rejected/superseded decisions from plan review.

- `open-questions.md`
  Open decisions that must be answered before MVP milestone approval or later milestones.

1. `2026-07-01-01-product-logic.md`
   Product logic, enterprise-vs-consumer positioning, run lifecycle, client surfaces, skills, memory, billing, approvals, and self-evolving boundaries.

2. `2026-07-01-02-technical-architecture.md`
   Overall architecture, service boundaries, API contract, data model, agent loop, sandbox strategy, deployment, and acceptance criteria.

3. `2026-07-01-03-storage-identity-memory.md`
   PostgreSQL/Redis/S3 placement, password hashing, RBAC, short-term memory, long-term memory, storage catalog, and API endpoints.

4. `2026-07-01-04-knowledge-rag-memory.md`
   Knowledge package, document ingestion, ACL-aware retrieval, candidate LlamaIndex adapter, memory candidate review, and runtime context loading.

5. `2026-07-01-05-skills-tool-gateway.md`
   Skill manifest validation, skill registry persistence, Tool Gateway, policy, approvals, billing, audit, and skill APIs.

6. `2026-07-01-06-agent-runtime-sandbox.md`
   Runtime state persistence, sandbox adapter, sandbox execution, browser session seam, bounded multi-agent delegation, and candidate LangGraph execution path.

7. `2026-07-01-07-billing-audit-observability.md`
   Billing service, pricing, append-only audit, trace spans, cross-service integration, and admin query APIs.

8. `2026-07-01-08-client-portal-creao-ui.md`
   Minimal CREAO-compatible static workspace slice for chat, run timeline, sandbox terminal output, artifacts, and approvals, plus later portal/admin/marketplace handoff notes.

9. `2026-07-01-09-deployment-operations.md`
   Docker Compose, API container, migration runner, Kubernetes cloud PoC manifests, worker separation, and operations runbooks.

10. `2026-07-01-10-security-compliance.md`
    Policy package, tenant isolation, secrets boundary, data classification, tool/network guardrails, and compliance audit coverage.

11. `2026-07-01-11-testing-release-quality.md`
    Test taxonomy, backend and frontend quality gates, integration harness, CI pipeline, release checklist, and rollback documentation.

12. `2026-07-01-12-self-evolving-evaluations.md`
    Evaluation service, failure taxonomy, improvement candidates, approval flow, versioned publication, eval APIs, and hard safety boundaries.

13. `2026-07-01-13-enterprise-onboarding.md`
    Enterprise tenant onboarding, default roles, SSO/SCIM planning, starter packs, quotas, readiness checks, and rollout runbooks.

14. `2026-07-01-14-api-sdk-contracts.md`
    API versioning, error model, pagination, idempotency, run event streaming, OpenAPI export, SDK shape, and webhooks.

15. `2026-07-01-15-enterprise-connectors.md`
    Enterprise SaaS/database/internal API/MCP connectors, credential boundaries, sync jobs, ACL mapping, Tool Gateway integration, audit, and billing.

16. `2026-07-01-16-sharing-collaboration-artifacts.md`
    Share grants, artifact delivery, run collaboration, output promotion, sharing UX, audit, billing, and retention controls.

17. `2026-07-01-17-model-gateway-provider-governance.md`
    Model Gateway, provider adapters, tenant model policy, credential boundaries, budgets, rate limits, metering, fallback, and runtime integration.

18. `2026-07-01-18-triggers-scheduling-automation.md`
    Schedules, webhooks, API triggers, connector event triggers, agent-to-agent handoffs, trigger admin APIs, audit, and billing.

19. `2026-07-01-19-agent-builder-workflow-templates.md`
    Agent templates, workflow graph model, input forms, preview runs, publication, run-to-template conversion, and builder API/UI contracts.

20. `2026-07-01-20-data-lifecycle-backup-recovery.md`
    Data inventory, lifecycle policy, retention jobs, tenant export, offboarding, backup/restore, disaster recovery, and data residency.

21. `2026-07-01-21-prompt-guardrail-governance.md`
    Prompt registry, prompt versioning, variable/secret handling, guardrail rules, prompt-injection checks, runtime/model integration, and publication gates.

22. `2026-07-01-22-incident-slo-support-operations.md`
    Incident models, SLO/error budgets, alert routing, run quarantine, kill switches, support access, and postmortem improvement linkage.

23. `2026-07-01-23-solution-packs-customer-success.md`
    Solution pack registry, tenant seeding, industry baselines, rollout playbooks, training assets, adoption metrics, and feedback loops.

24. `2026-07-01-24-private-deployment-packaging.md`
    BYOC/private package manifest, config profiles, Helm path, license entitlements, install validation, upgrades, rollback, and air-gapped runbooks.

25. `2026-07-01-25-roadmap-coverage-matrix.md`
    Review roadmap, source-requirement coverage matrix, phased execution order, dependency map, MVP recommendation, and evidence gates.

26. `2026-07-01-26-mvp-cloud-poc-execution.md`
    First backend-focused implementation milestone for the cloud PoC: route contracts, persistence, auth, knowledge/memory, skill/tool gateway, model gateway, sandbox seam, governance hooks, onboarding, frontend contract freeze, deployment, and end-to-end acceptance.

27. `2026-07-01-27-plan-review-approval-workflow.md`
    Plan review status model, review groups, reviewer checklist, decision log, open questions, MVP approval gate, change control, and implementation handoff.

Current implementation status:

- Phase 1 API foundation is started.
- API idempotency is started for `POST /api/runs`, `POST /api/runs/{run_id}/approvals`, `POST /api/runs/{run_id}/approvals/reject`, `POST /api/triggers/{trigger_id}/webhook`, and restore drill execution enqueue with tenant-scoped `Idempotency-Key` or webhook delivery-id replay, payload/body-hash conflict detection, SQL-backed `idempotency_records`, and `docs/contracts/idempotency-contract.md` for SDK/client handoff.
- API pagination is started with Pydantic `PageRequest`/`PageResult` models, tenant-scoped `GET /api/runs` cursor pages, run status/workspace filters, paged `GET /api/billing/meters`, `GET /api/audit-events`, `GET /api/skills`, `GET /api/memory`, `GET /api/memory/short-term`, `GET /api/runs/{run_id}/artifacts`, `GET /api/runs/{run_id}/storage-objects`, `GET /api/knowledge-bases`, and `GET /api/knowledge-documents` when pagination parameters are supplied, and `docs/contracts/api-pagination-contract.md` for SDK/client handoff.
- Run event stream contract is started with per-run `sequence`, SSE `id:` values, `after_sequence` replay, `Last-Event-ID` reconnect support, and `docs/contracts/run-event-stream-contract.md` for frontend handoff.
- License import contract is started with `POST /api/licenses/import`, `licenses.manage`, tenant mismatch rejection, sanitized response shape, and `license.imported` audit requirements in `docs/contracts/license-import-contract.md`.
- Agent runtime foundation is started, including runtime state snapshots exposed through `GET /api/runs/{run_id}/state`, retry, cancellation and approval resume/rejection after process-local pending state is lost, SQL-backed approval/status/artifact persistence through the control-plane repository, run cancellation with pending approval cancellation, and persisted model-planning plus artifact-publication guardrail approval resume.
- OpenAI-compatible Model Gateway contract and runtime boundary are started, including runtime planning `model_call_count`, input-token, output-token, safe model-plan audit records when usage is returned, first-pass Settings-backed plus API/SQL-managed global and tenant/workspace-scoped allowed/denied model policy plus scoped default models, staged model policy change-request approvals that keep pending scopes out of runtime until approval, SQL-backed tenant/provider rate-limit samples shared by API and worker router instances when configured, Pydantic `TAROAI_MODEL_GATEWAY_CHAT_REQUEST_OPTIONS` plus provider-level `chat_request_options` for provider-specific OpenAI-compatible payload options such as JSON response format, safe model-policy admin audit records, safe `model.policy_denied` audit records for policy rejections, run/tenant/workspace/user/agent model call/token budget guard, `/readyz` model gateway configuration readiness without key disclosure or provider calls, safe `model.gateway_failed` audit records when gateway configuration or response failures fail a run, model request/response guardrail enforcement at the runtime gateway boundary, and provider-compatible payload construction that omits `tool_choice` unless tool schemas are declared.
- Tool Gateway foundation is started with Pydantic request/policy/result/audit-record/secret-requirement models, registered-handler execution, scope checks, approval-required decisions, input/output schema validation, runtime context invocation, short-lived secret lease injection, unified API error mapping, service-level `tool.blocked` and `tool.approval_required` audit recording through injected `AuditService` with sensitive input redaction, guardrail block/approval/redaction enforcement before handler execution, runtime policy-approval pause, successful tool-call audit/billing records, and failed runtime tool-call audit with sensitive input redaction.
- Knowledge/RAG foundation is started with Pydantic models, in-memory and SQLite-compatible SQL knowledge metadata/chunk persistence, tenant-scoped knowledge deletion, ACL-aware retrieval, citation results, API endpoints behind `knowledge.write`/`knowledge.read`, safe audit events for base/document/query operations, and Agent Runtime context loading with sanitized `context.loaded` events plus retrieval guardrail filtering before model planning.
- Sandbox/Browser foundation is started with Pydantic session/command/file/snapshot/browser models, disabled default sandbox/browser provider boundaries, explicit `local_process` sandbox provider for local cloud PoC command execution inside per-session workspaces, first-pass Docker provider selection through Pydantic settings and the shared factory path with disabled-network containers, Settings-managed memory/CPU/pids limits, non-root `--user`, read-only rootfs, `cap-drop=ALL`, `security-opt`, tmpfs mounts, workspace bind mounts, Docker user namespace/rootless-compatible workspace permissions, `docker exec` command execution, file upload/download/list/snapshot, destroy lifecycle, and live Docker provider verifier coverage, Settings-backed HTTP browser provider adapter for `playwright`/`browserbase` controller services, first-pass Playwright HTTP browser controller service with session/action/screenshot contract and local Compose wiring, Tool Gateway command handler, runtime `sandbox.command` and `browser.action` session creation/session-id injection, persisted sandbox/browser session IDs in runtime state snapshots, declared sandbox file promotion only under `/workspace/artifacts/` or automatic `/workspace/artifacts/**` discovery into storage-backed run artifacts, rejection of declared artifact paths outside `/workspace/artifacts/`, artifact-stage guardrail content evaluation and storage content scanning before runtime artifact upload, safe `tool_call.completed` summaries without raw sandbox stdout/stderr or browser text, success/failure/approval-rejection/cancellation cleanup for runtime-created sessions, sandbox command lease-handle environment delivery without raw secret values, sandbox-scoped lease resolution API with run/step/session validation, optional provider token enforcement, and safe audit metadata, API endpoints for session create, command execution, file upload/download, snapshot, destroy, and browser actions, `sandbox.create`/`sandbox.execute`/`browser.act` permission checks, session/command/file/snapshot/destroy/browser audit metadata, sandbox command output object upload, sandbox file object upload, sandbox snapshot JSON object upload, browser screenshot object upload with non-serialized screenshot bytes, `sandbox_minutes` command metering, `artifact_bytes` file-upload metering, and `browser_action_count` metering when a run exists. Local contract adapters stay under `tests/` and are injected only by tests; browser action audit records typed-text presence and length without raw typed text.
- Secrets boundary foundation is started with Pydantic `SecretRef`, `SecretLease`, `SecretScope`, `SecretLeaseResolveRequest`, `SecretLeaseResolution`, and AWS Secrets Manager config models, scoped short-lived lease checks, Tool Gateway short-lived lease injection before handler execution with run/step/session context, sandbox command lease-handle environment delivery with reserved-key override protection, sandbox-scoped lease resolution through `SecretService` strict bound-context validation, optional `X-Sandbox-Resolver-Token` enforcement through `TAROAI_SANDBOX_SECRET_RESOLVER_TOKEN`, local and AWS-backed value storage selected through settings, long-lived values kept out of model dumps, and audit metadata that excludes secret values and lease tokens.
- Policy foundation is started with Pydantic `PolicyRequest`, `PolicyDecision`, and `PolicyEffect` models, an `IdentityPolicyService` that centralizes first-pass RBAC decisions, and FastAPI tenant-operation permission checks routed through `app.state.policy_service`.
- Guardrail foundation is started with Pydantic stage/action/severity/rule/condition/evaluation/decision/finding models, tenant/workspace-scoped in-memory rule evaluation for allow, warn, redact, approval-required, block, and quarantine decisions, Settings-backed built-in secret-pattern, prompt-threat, and HTTP guardrail detector boundaries, Tool Gateway request enforcement, Agent Runtime retrieval-context enforcement, model request/response enforcement, persisted model-planning guardrail approval resume, artifact metadata and sandbox artifact content publication enforcement with persisted approval resume, long-term memory candidate guardrail approval review, SQL-backed short-term memory review queue approval/rejection, long-term/short-term memory-write enforcement with safe audit metadata, and sanitized guardrail finding summaries in run traces, plus default API/worker runtime guardrail wiring. Remaining guardrail work: provider-specific guardrail integration policy and broader semantic threat coverage.
- Audit foundation is started with Pydantic `AuditEventCreate`, `AuditActor`, `AuditResource`, `AuditAction`, and audit coverage matrix/report models, an `AuditService` that records through the control-plane store, recursive sensitive metadata redaction, defensive audit-event copies, request actor attribution for FastAPI business audit writes, Agent Runtime, Tool Gateway, and worker job actor attribution from tenant/user context, tenant coverage checks against default enterprise requirements, `GET /api/audit-events/coverage` behind `audit.read`, FastAPI business audit writes/list reads routed through `app.state.audit_service`, Agent Runtime model/tool audit events, Tool Gateway service-level blocked/approval-required audit events, worker job started/succeeded/failed audit events, tenant bootstrap completion audit events, identity user/role lifecycle audit events, billing meter audit events, run-cancellation/retry audit events, approval-resolution/rejection audit events, and skill-publication audit events through injected service boundaries.
- DB persistence foundation is started with Pydantic database config, migration runner, runtime state table, a shared SQLite/PostgreSQL connection factory, psycopg-backed PostgreSQL URL support, process-level PostgreSQL connection pools configured through Pydantic min/max/timeout settings, PostgreSQL tenant session context setting before tenant-scoped SQL, PostgreSQL-only RLS migration blocks for tenant-scoped tables, live PostgreSQL migration/RLS verification against a non-superuser app role, SQL parameter/upsert compatibility tests for existing repositories, SQLite-compatible SQL repository coverage for run/event/status/artifact/approval/meter/audit/runtime-state persistence, and settings-based FastAPI app plus worker runner wiring for the SQL control-plane store.
- Local deployment foundation is started with Docker Compose for Web, API, browser-controller, PostgreSQL, Redis, and MinIO services, an API Dockerfile, browser-controller Dockerfile, and entrypoint with migration execution, `/healthz` and `/readyz` endpoints including model gateway configuration readiness, a non-superuser PostgreSQL app role initialized through `infra/postgres/init.sql`, configurable host ports, a Pydantic `.env.example` local cloud PoC template with Docker sandbox non-root user configuration, `docs/operations/mvp-local-cloud-poc.md`, OpenAI-compatible model provider verification, local PoC smoke verifier coverage for API/workspace auth UI and Bearer-auth script contract, backend auth/readiness/run diagnostics/sandbox command/browser extract, optional browser-controller workspace loading through `--browser-workspace-url`, optional Chromium-driven workspace login through `--browser-workspace-api-base-url` with `Bearer` auth-status verification, plus optional succeeded-run/required-artifact/storage-object/content-download/run-event proof with artifact event storage-object matching and raw sandbox-output leak rejection when a real model provider is configured, live Docker Compose smoke verification for Web/API/browser-controller/PostgreSQL/Redis/MinIO on alternate host ports, live Docker sandbox provider verification, live PostgreSQL migration/RLS verification, live Redis worker queue verification including expired-lease recovery and dead-letter behavior, live Redis-backed short-term memory verification, live MinIO/S3-compatible object storage verification against the Compose bucket, and an API-level MVP acceptance scenario in `tests/api/test_mvp_end_to_end.py` plus `docs/mvp/acceptance-scenario.md`. The SQL repository layer now accepts PostgreSQL URLs through the shared connection adapter and uses a process-level psycopg pool configured through Pydantic settings; first-pass migration tooling can emit a JSON plan for pending/unknown migrations before explicit apply.
- Worker/queue foundation is started with Pydantic run execution, billing aggregation, cleanup, connector-sync, trigger-due, trigger-scheduler, restore-drill-due, restore-drill-execution, restore-drill-evidence, and restore-drill-scheduler job contracts and worker runners; queue claim/ack/fail/reject lifecycle tests; retry/dead-letter policy; Redis-backed queue adapter boundary; configurable API enqueue mode for run execution; worker job lifecycle audit events with actor attribution; a Pydantic worker runner/entrypoint with settings-based store, knowledge-service, connector-registry, restore-drill schedule store, storage catalog, and object-storage selection; worker-kind CLI for agent, cleanup, connector-sync, trigger-due, trigger-scheduler, restore-drill-due, restore-drill-execution, restore-drill-evidence, and restore-drill-scheduler execution; injected `AuditService`; and default runtime tool handlers, plus a cleanup worker runner for storage lifecycle cleanup, Redis worker queue verifier, first-pass Kubernetes worker process manifests, first-pass Kubernetes manifests for API, migration, PostgreSQL, Redis, MinIO, bucket initialization, and NetworkPolicy runtime traffic controls using shared runtime ConfigMap/Secret boundaries, plus a first-pass Helm chart path for API, workers, migration, config, external Secret references, ingress, autoscaling, service accounts, and network policies.
- Private deployment packaging is started with a Pydantic deployment package manifest, JSON Schema, package README, required service coverage for API, worker, database, Redis, object storage, sandbox provider, browser controller, Web Workspace, model gateway, and secrets manager, deployment mode settings, customer-operated profile validation, air-gapped external-provider rejection, cloud/BYOC/private env profile examples, API/worker/browser-controller/Web Workspace release image baseline checks, required Web Workspace package entries for Dockerfile, static assets, Kubernetes manifest, Helm values, and Helm web template, first-pass Kubernetes and Helm Web Workspace manifests using the `taroai-web` image without runtime secret mounts, a Pydantic licensing/entitlement service with offline license file validation, Ed25519 signed offline license envelope validation, signed license import API, SQL-backed active license validation persistence through the control-plane store, connector-count, sandbox-concurrency, audit-retention, solution-pack-install, SSO-provider-config, and SCIM-provider/import runtime enforcement, and `license.status_changed`/`license.imported` audit events, a Pydantic install validation report contract with executable release-package integrity, detached Ed25519 release signature verification, migration-plan, object-storage verification, Redis connectivity, worker-queue, secret-manager, model gateway, sandbox lifecycle, browser-controller lifecycle, optional Web Workspace health through `--web-base-url`, event-stream, audit-write, trace-collector, and backup-restore-drill verification evidence inputs, dependency evidence harnesses for migration plan, object storage, Redis queue, secret manager, event stream, audit write, model gateway, sandbox lifecycle, browser-controller lifecycle, trace collector, and restore drill, non-sensitive API evidence traceability fields such as `api_base_url`, `run_id`, and `first_event_sequence`, a restore drill evidence builder, a scheduled restore drill due-job scheduler plus SQL-backed due-worker request-record intake with lifecycle API review/execution-enqueue/status-evidence update endpoints, a first-pass restore-drill execution worker that invokes the verifier and enqueues governed evidence collection, a first-pass restore-drill evidence collection worker that stores successful verifier output as managed data-export evidence or marks failed verification terminal, an operator runbook, private upgrade/rollback runbook, air-gapped install runbook, and upgrade matrix through `032_solution_pack_publication_draft_multi_manifest`. Remaining actual restore environment orchestration, full portal/admin frontend packaging, automated release signing/key-rotation operations, and customer-specific compatibility expansion remain planned.
- Trigger automation foundation is started with Pydantic tenant-scoped trigger definitions for schedule, webhook, API, connector event, and agent handoff sources; schedule config with cron/timezone/start/end/catch-up cap; connector-event config with connector id, event type, and payload match conditions; agent-handoff config with target agent, max depth, and required permission actions; schedule evaluation that emits formal `triggers.due` job payloads; scheduler worker queue insertion with `next_run_at` advancement and safe `trigger.schedule.evaluated` audit metadata; `TriggerDue` worker consumption into accountable autonomous runs plus `runs.execute` queue jobs; SQLite/PostgreSQL-compatible trigger, connector-event, and handoff config persistence through `trigger_store_backend`; `trigger_scheduler` and `trigger_due` CLI/Kubernetes worker deployment; Settings-managed signed webhook verification with HMAC-SHA256 raw-body signatures and replay tolerance; webhook delivery-id idempotent replay/conflict handling without raw payload storage; generic connector event ingest/matching behind `triggers.invoke`; bounded agent handoff execution with source/target run events; accountability validation for user or service-account context; FastAPI create/list/get/enable/disable/invoke/webhook/connector-events/agent-handoff endpoints; trigger operations visibility through `GET /api/triggers/operations`; accountable autonomous run creation through the existing control-plane path; safe `trigger.invoked` audit metadata; and `trigger_invocation_count` billing meters. Provider-specific connector adapters/ACL mapping, advanced handoff approval policy, tenant/workspace-level signing secret rotation, alert integration, and automated remediation remain planned.
- Storage foundation is started with a tenant-scoped metadata catalog, SQLite-compatible SQL metadata catalog wired through Pydantic settings, S3/MinIO-compatible object storage adapter boundary, upload/download/delete/signed URL contracts, FastAPI endpoints for metadata registration, run-scoped listing, signed URL creation, object content upload with declared-size validation, object content download, tenant/workspace-scoped internal objects, object `acl_subjects`/`sensitivity_level` with read-side enforcement, configurable upload content scanning, knowledge document source object upload, retention-aware object delete, first-pass expired-object cleanup lifecycle service with preview mode, sandbox command output object upload, sandbox file object upload, sandbox snapshot JSON object upload, browser screenshot object upload, default-off external artifact link download through active `external_link` share grants, and MinIO/S3-compatible object storage verifier. Storage endpoints enforce `storage.read`/`storage.write`; upload writes through the object storage adapter and records `storage_bytes` billing plus `storage.uploaded` audit metadata without raw object content; rejected uploads record `storage.content_rejected` metadata without raw content or rule text; authenticated download reads through the object storage adapter and records `storage.downloaded` audit metadata without raw object content; external-link share grant API responses redact the link token, external-link grant creation rejects non-artifact resources, link tokens shorter than 32 characters, and non-`view` permissions, newly created external-link grant subjects are stored as tenant-scoped keyed `hmac-sha256:` digests, and external-link downloads reject sensitive artifacts while recording token-redacted `storage.downloaded` audit plus `external_artifact_download_bytes` meter metadata; signed URL creation records `storage.signed_url.created` audit metadata without the generated URL; delete writes metadata tombstones and `storage.deleted` audit metadata; cleanup deletes expired active objects through the adapter, skips active legal holds, supports preview without adapter delete or metadata tombstone, and records system audit metadata. Tenant-wide billing/audit/trace reads now require `billing.read`/`audit.read`; billing reads support first-pass filters and grouped summaries through `apps/api/src/taroai/billing/`, and run trace aggregation/export with first-pass `TraceSpan`/`TraceEvent` entries, runtime stage spans, sanitized guardrail findings, HTTP exporter boundary, error classification, OTLP HTTP trace collector deployment verification, and install-validation trace collector evidence has started through `apps/api/src/taroai/observability/`.
- Lifecycle governance foundation is started with Pydantic data category, deletion behavior, lifecycle policy, legal hold, data export manifest/bundle, backup manifest, data residency report, tenant offboarding plan, restore drill schedule, and restore drill run-record models plus in-memory and SQLite-compatible SQL stores selectable through Settings, tenant default plus workspace override policy resolution, FastAPI lifecycle policy/legal-hold/export/backup-manifest/data-residency/offboarding/restore-drill APIs including restore drill run-record status/evidence updates, storage cleanup preview API, RBAC permissions, storage-object export manifests, tenant/workspace/run scoped JSON export bundles uploaded through the object storage adapter, safe backup manifests without raw connection strings, data residency settings for primary/allowed regions and replication mode, tenant offboarding request planning with legal-hold blocking, approval advancement, approved tenant-scoped export bundle execution, first-pass storage-object deletion execution, first-pass memory tombstone/delete execution, first-pass knowledge metadata/chunk deletion execution, and audit events that omit raw legal-hold reason text, export item details, backup component details, full residency check details, raw offboarding reason text, deleted storage object IDs, deleted memory record IDs, deleted knowledge document IDs, legal-hold IDs, and restore verifier input paths.
- Identity/auth/onboarding foundation is started with Pydantic identity services, password hashing, disabled-user handling, SQL-backed users/roles/role assignments through settings, signed PoC access tokens with server-side session IDs, `/api/auth/login`, `/api/auth/logout`, Bearer request-context resolution, SQL-backed auth session persistence/revocation when SQL identity is used, dev request headers behind settings, and token-protected `/api/tenants/bootstrap` for slug-derived first-owner, starter workspace, starter knowledge, and starter skill-pack seeding.
- Tenant readiness foundation is started with Pydantic readiness report/check models, owner/role/auth/quota/audit/billing/storage/queue checks, and `/api/tenants/current/readiness` behind authenticated request context. Skill registry foundation is started with tenant-scoped lifecycle records, tenant/department/workspace/private visibility filtering, version history lookup, workspace install/enable/disable records, SQLite-compatible SQL persistence through settings, `/api/skills` register/list/get/publish/disable endpoints, `GET /api/skills/{skill_id}/versions`, workspace skill install/list/enable/disable endpoints, and dev-mode workspace skill invocation through Tool Gateway with manifest scope enforcement, safe audit metadata, and `skill_call_count` billing behind identity permissions. Memory foundation is started with Pydantic short-term TTL memory, Redis-backed short-term put/get/list/delete with TTL through settings, Redis-backed short-term memory verifier, long-term scoped memory, candidate approve/reject review, memory read/write/review APIs with audit metadata, runtime context loading, and SQLite-compatible SQL long-term memory persistence through settings.
- Connector foundation is started with Pydantic connector type/status/auth/capability/definition models, credential-reference-only auth metadata, tenant/workspace-scoped memory and SQLite/PostgreSQL-compatible SQL registry boundaries, FastAPI create/list/get/update/enable/disable APIs behind `connectors.manage` and `connectors.read`, safe `connector.registered`, `connector.updated`, `connector.enabled`, and `connector.disabled` audit metadata, migration/RLS coverage, persisted connector sync state/cursor records, sync ACL planning that maps source principals into knowledge ACL subjects without direct long-term memory writes, `connectors.sync` job enqueue/worker execution into knowledge with a dedicated `connector_sync` worker kind, safe audit plus `connector_sync_document_count` billing, run-scoped connector capability invocation decisions behind `connectors.invoke` with safe audit metadata plus `connector_invocation_count` billing for ready decisions, approval-required connector invocation linkage to persisted run approval requests with approved execution gating, internal API HTTP dispatch with method/path allowlists plus API-key/OAuth2 bearer access-token injection through short-lived secret leases, read-only database connector dispatch with secret-referenced DSNs, SELECT-only enforcement, table allowlists, safe failure audit, OAuth authorize/callback/refresh management with token rotation through secret references, and AWS Secrets Manager value storage through the same lease API.
- The plan set now covers product logic, technical architecture, storage/identity/memory, knowledge/RAG, skills/tool gateway, agent runtime/sandbox, billing/audit/observability, client portal UI, deployment, security, quality gates, self-evolving evaluations, enterprise onboarding, API/SDK contracts, enterprise connectors, sharing/collaboration, model gateway, triggers/scheduling, builder/workflow templates, data lifecycle/DR, prompt/guardrail governance, incident/SLO operations, solution packs/customer success, private deployment packaging, roadmap/coverage review, MVP cloud PoC execution, and plan review/approval workflow.
- OIDC/SAML login flow and assertion validation, full SCIM v2 service-provider compatibility, MFA, full tenant onboarding orchestration, selectable industry starter packs, IdP/SCIM-backed storage subject mapping, multipart upload, production DLP/AV scanning adapters, non-storage lifecycle cleanup categories beyond memory, tenant-wide asynchronous export orchestration, broader offboarding deletion orchestration, backup execution, actual restore environment orchestration, advanced PostgreSQL migration orchestration beyond plan/apply, Ingress/TLS/cloud-managed-service Kubernetes hardening beyond the first-pass manifests and Helm chart path, full Knowledge/RAG integrations for embeddings, vector backend, advanced durable ingestion policy, and advanced context policy, full Tool Gateway integrations for connectors, sandbox provider network isolation and mTLS/IAM hardening, skill-specific meters, remaining audit matrix event producers, bootstrap/identity/system actor attribution, Kubernetes/E2B/microVM-backed sandbox isolation, production filesystem hardening, browser live-view frontend, physical multi-region data residency enforcement, broader ABAC/risk policy, broader distributed budget governance, advanced billing/observability services, evaluation workflows, API SDKs, SaaS/file/MCP provider-specific connectors, provider-specific OAuth edge cases, broader database dialect/query governance, tenant-specific KMS/IAM policy hardening, additional secret backend providers, sharing workflows, provider-specific trigger connector delivery, builder API, prompt registry, provider-specific guardrail integration policy, incident ops, support access, industry solution-pack content and success workflows, production SSO/MFA frontend, full admin/skill-marketplace frontend, automated release signing/key-rotation operations, and customer-specific compatibility expansion remain implementation work.
