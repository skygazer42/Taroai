# Taro Pet Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Create, validate, and install a Codex-compatible v2 soft-3D chibi pet named Taro from `F:\cool\jiangning\VQQY5110.JPG`.

**Architecture:** Use `@hatch-pet` as the orchestration contract and `@imagegen` as the only visual-generation layer. Lightweight workers each generate one bounded visual job; the parent copies approved sources, runs deterministic Pillow-based extraction and validation, assembles the 8x11 atlas, performs independent direction and contact-sheet QA, and installs the passing package under the Codex pets directory.

**Tech Stack:** Codex built-in image generation, hatch-pet Python/Pillow scripts, JSON manifests, PNG/WebP spritesheets, Codex pet schema v2

---

### Task 1: Prepare the Taro run

**Files:**
- Reference: `F:\cool\jiangning\VQQY5110.JPG`
- Reference: `C:\Users\luke\Desktop\Taroai\docs\plans\2026-07-11-taro-pet-design.md`
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\pet_request.json`
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\imagegen-jobs.json`

**Step 1: Load the bundled workspace dependencies**

Call `codex_app__load_workspace_dependencies` and retain the exact Python executable as `PYTHON`. Do not use bare `python`.

**Step 2: Prepare the run manifest**

Run `prepare_pet_run.py` with pet name `Taro`, the approved description, the supplied reference image, style preset `3d-toy`, and the exact run directory above.

**Step 3: Inspect readiness**

Read `imagegen-jobs.json`. Expected: `base` is ready, later jobs have explicit dependencies, prompt files, layout guides, input images, and decoded output paths.

**Step 4: Verify preparation**

Confirm the request uses a chroma key absent from the black, stone, brown, and skin-tone palette; confirm every row has the required guide and stable identity prompt.

### Task 2: Generate and approve the canonical base

**Files:**
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\decoded\base.png`
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\references\canonical-base.png`

**Step 1: Dispatch one base worker**

The worker reads `prompts/base-pet.md`, attaches the supplied photo as identity reference, and uses `@imagegen` only.

**Step 2: Validate the selected source**

Require one centered full-body soft-3D chibi human, recognizable glasses/smile/hair/jacket, flat chroma background, no scenery, text, logos, shadows, or detached effects.

**Step 3: Copy and record provenance**

Copy the selected source to `decoded/base.png` and `references/canonical-base.png`, then mark only the `base` job complete with its source path and timestamp.

### Task 3: Generate and incrementally validate standard rows

**Files:**
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\decoded\{idle,running-right,running-left,waving,jumping,failed,waiting,running,review}.png`
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\qa\rows\*\review.json`

**Step 1: Generate identity and gait checks**

Dispatch separate workers for `idle` and `running-right`, each attaching the canonical base and its layout guide.

**Step 2: Run row extraction and inspection**

For each selected row, run `extract_strip_frames.py --method auto` and `inspect_frames.py --require-components`. Expected: no errors, correct frame count, complete separated poses, and consistent identity.

**Step 3: Derive or generate running-left**

If glasses, jacket, hair, and all markings are mirror-safe, use `derive_running_left_from_running_right.py --confirm-appropriate-mirror`; otherwise generate the row independently.

**Step 4: Generate remaining rows**

Keep up to three independent workers active for `waving`, `jumping`, `failed`, `waiting`, `running`, and `review`. Copy and validate each row before marking it complete.

**Step 5: Repair only genuine source failures**

Use deterministic stable-slot extraction for extraction-only instability. Regenerate only a complete failing row when its source semantics, identity, clipping, or effects are wrong.

### Task 4: Assemble and review the standard atlas

**Files:**
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\final\spritesheet.webp`
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\qa\contact-sheet.png`
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\qa\previews\*.gif`
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\qa\review.json`

**Step 1: Extract all standard frames**

Run `extract_strip_frames.py --states all --method auto`.

**Step 2: Inspect and compose**

Run `inspect_frames.py --require-components`, `compose_atlas.py`, `make_contact_sheet.py`, and `render_animation_previews.py`.

**Step 3: Review at normal pet size**

Confirm consistent identity and state semantics, alternating directional gait, visible idle micro-motion, and no size popping or reversed timing.

### Task 5: Define and approve Taro's look mechanics

**Files:**
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\qa\look-mechanics.md`
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\decoded\look-anchors-approved.png`

**Step 1: Write the mechanics decision**

Specify eyes leading, eyelid/eyebrow participation, small rigid head/neck turns, restrained upper-torso follow-through, fixed lower body, rigidly attached glasses, and slight collar/hair lag. Prohibit skull warping and whole-sprite rotation.

**Step 2: Generate the cardinal strip**

Dispatch one worker for `look-cardinals` in order `000 up`, `090 screen-right`, `180 down`, `270 screen-left`.

**Step 3: Extract and approve anchors**

Run `extract_cardinal_anchors.py` and `compose_cardinal_anchor_strip.py`. Confirm each cardinal from visible eye, nose, face, and head landmarks at final size.

### Task 6: Generate and validate both coherent look rows

**Files:**
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\decoded\look-row-9.png`
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\decoded\look-row-10.png`
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\qa\look-row-9-registration.json`

**Step 1: Generate row 9**

Dispatch one worker using the approved cardinals and standard contact sheet for `000` through `157.5`.

**Step 2: Register and gate row 9**

Run `assemble_extended_atlas.py` in registered-row mode and inspect edge, semantic, and continuity evidence. Regenerate the complete row for any hard failure.

**Step 3: Generate row 10**

Only after row 9 passes, dispatch one worker using the approved cardinals plus completed row 9 for `180` through `337.5`.

### Task 7: Assemble, clean, and deterministically validate v2

**Files:**
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\final\spritesheet-extended.webp`
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\final\validation-extended.json`
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\qa\chroma-despill-extended.json`

**Step 1: Assemble the two look rows**

Run `assemble_extended_atlas.py` with approved registered row 9, row 10, the idle neutral cell, and the manifest chroma key.

**Step 2: Apply the single permitted despill pass**

Run `despill_chroma_edges.py` once on the complete 8x11 atlas. Expected: `ok: true`.

**Step 3: Validate the atlas**

Run `validate_atlas.py --require-v2`. Expected: exactly `1536x2288`, all used cells non-empty, unused cells transparent, and no structural or chroma errors.

### Task 8: Complete independent visual and direction QA

**Files:**
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\qa\contact-sheet-extended.png`
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\qa\look-directions.png`
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\qa\direction-semantics.json`
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\qa\direction-blind-validation.json`
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\qa\look-continuity.json`

**Step 1: Create QA artifacts**

Run the contact-sheet, focused-direction-sheet, blind-pair-sheet, and continuity scripts.

**Step 2: Run three isolated blind reviews**

Each fresh worker sees only `direction-blind-pairs.png`. Combine strict-majority verdicts and validate them against the hidden answer key. Cardinals are hard gates.

**Step 3: Run final visual QA**

Dispatch one independent worker to inspect the cleaned contact sheet, direction sheet, previews, semantics, blind validation, continuity, and deterministic validation. Repair complete rows for major failures only.

### Task 9: Install and verify Taro

**Files:**
- Create: `C:\Users\luke\.codex\pets\taro\pet.json`
- Create: `C:\Users\luke\.codex\pets\taro\spritesheet.webp`
- Create: `C:\Users\luke\.codex\pet-runs\taro-20260711\qa\run-summary.json`

**Step 1: Install the approved atlas**

Copy `spritesheet-extended.webp` to the pet package and write `pet.json` with `id: "taro"`, the approved display name and description, `spriteVersionNumber: 2`, and `spritesheetPath: "spritesheet.webp"`.

**Step 2: Verify the installed pair**

Re-run v2 atlas validation against the installed spritesheet and parse `pet.json`. Expected: both pass and refer to the same package.

**Step 3: Write the run summary and clean intermediates**

Keep the required final QA evidence and remove disposable prompts, guides, row strips, extracted frames, PNG intermediates, the standard atlas, and the job manifest.

**Step 4: Report completion**

Return the package path, run-summary path, contact sheet, direction sheet, and deterministic validation result.
