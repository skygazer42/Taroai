# Review Readiness Audit

This audit is the evidence map for reviewing `/data/temp34/Taroai/docs/plans`.

It answers:

1. Which explicit user requirements are covered by the plan set?
2. Which files prove the coverage?
3. Which decisions still need human review before the next implementation milestone is approved?
4. Which current-code facts should not be overstated as shipped platform capability?

## Status Summary

| Area | Status | Evidence |
| --- | --- | --- |
| Plan coverage | Ready for human review | 27 dated implementation plans plus review artifacts. |
| MVP milestone approval | Not yet approved | `open-questions.md` still has Q-001 through Q-008 open. |
| Product-flow model boundary | Aligned and started | Product/MVP plans require OpenAI-compatible Model Gateway; current backend runtime uses that boundary. |
| Test fixture language in plans | Clean | Evidence gate rejects prototype/test provider names in product-flow wording. |
| Current backend foundation | Started, not full platform | FastAPI/Pydantic service foundations, runtime state snapshots, Model Gateway contract with runtime request/response guardrails and `/readyz` configuration readiness, OpenAI-compatible Embedding Gateway usage audit plus run-scoped and operation-level meters, Tool Gateway foundation with schema validation, optional blocked-call audit recording, runtime PolicyService scope resolution, runtime policy-approval pause, successful-call audit/billing, and failed-call audit redaction, Policy Service foundation, Guardrail foundation, Audit Service foundation, Secrets boundary foundation with AWS Secrets Manager value storage, Knowledge foundation, Sandbox/Browser foundation including explicit `local_process` local cloud PoC execution, first-pass Settings-hardened Docker disabled-network container execution, first-pass Playwright HTTP browser controller service, runtime-created sandbox artifacts from declared `/workspace/artifacts/**` paths or auto-discovered `/workspace/artifacts/**` files after artifact-stage guardrail content evaluation and storage content scanning, rejection of declared artifact paths outside `/workspace/artifacts/`, and success/failure/approval-rejection/cancellation cleanup for runtime-created sessions, trigger automation foundation with accountable API invocation and operations visibility, signed PoC auth/session with logout and SQL-backed session revocation, tenant readiness checks, SQL control-plane store wiring for API and worker runner, SQL storage metadata catalog with settings-based app wiring, object content upload/download paths, tenant default plus workspace override lifecycle policy resolution, storage cleanup preview, storage-object export manifests/bundles, safe backup manifests, and a minimal static workspace UI with Bearer login/logout controls exist; browser live-view frontend, production SSO/MFA UI, full admin/skill-marketplace frontend, and production frontend test stack remain implementation work. |
| External terminology grounding | Added | `research-grounding.md` records source-backed terminology and current-code limits. |

## Requirement Coverage

| Requirement | Primary Evidence | Audit Result | Notes |
| --- | --- | --- | --- |
| Plans first, review before milestone approval | `mvp-review-packet.md`, `review-status.md`, `review-decisions.md`, `open-questions.md`, `2026-07-01-27-plan-review-approval-workflow.md` | Covered for review | Future MVP slices should not proceed until impacted review groups move out of `draft`. |
| Enterprise Agent Cloud Workspace positioning | `2026-07-01-01-product-logic.md`, `2026-07-01-02-technical-architecture.md`, `2026-07-01-25-roadmap-coverage-matrix.md` | Covered | Plan 01 distinguishes enterprise delivery from consumer chat. |
| Cloud-first deployment with later private path | `2026-07-01-09-deployment-operations.md`, `2026-07-01-24-private-deployment-packaging.md`, `review-decisions.md`, `infra/docker-compose.yml`, `apps/api/Dockerfile`, `apps/web/Dockerfile`, `.env.example`, `.gitignore`, `docs/operations/mvp-local-cloud-poc.md`, `docs/operations/private-install-validation.md`, `docs/operations/private-upgrade-rollback.md`, `docs/operations/air-gapped-install.md`, `infra/k8s/`, `infra/helm/taroai/`, `infra/package/`, `infra/config/`, `apps/api/src/taroai/deployment/`, `apps/api/src/taroai/licensing/`, `apps/api/src/taroai/config.py` | Covered and partially implemented | Cloud PoC first is accepted; local Compose for Web/API/browser-controller/PostgreSQL/Redis/MinIO, API entrypoint, health/readiness endpoints, local PoC smoke verifier with optional succeeded-run/artifact enforcement for configured model providers, live Docker Compose smoke verification on alternate host ports, PostgreSQL migration/RLS verification, Redis worker queue and short-term memory verification, MinIO/S3 verification against the Compose bucket, first-pass Kubernetes manifests for API, worker, backing services, browser-controller, and Web Workspace, and NetworkPolicy runtime traffic allowlists are started. The package ships `.env.example` as the template and keeps `.env` local-only so a packaged override cannot disable the local PoC sandbox default. A first-pass Helm chart path exists for API, workers, Web Workspace, migration, config, external Secret references, ingress, HPA, service accounts, and network policies without literal secret defaults, including restore drill due, restore drill execution, restore drill evidence, and restore drill scheduler worker entries. The deployment package manifest, config profile validation, licensing/entitlement service, install validation report contract, restore drill evidence builder, scheduled restore drill due-job scheduler plus SQL-backed due-worker request-record intake, lifecycle API review/execution-enqueue/status-evidence update endpoints, and restore-drill execution worker verifier handoff and evidence worker ingestion, upgrade/rollback runbook, air-gapped install runbook, and upgrade matrix through `032_solution_pack_publication_draft_multi_manifest` are started with Pydantic models, JSON Schema, operator README, release image baseline checks for API/worker/browser-controller/Web Workspace, deployment mode settings, cloud/BYOC/private env examples, air-gapped external-provider rejection, offline license file validation, Ed25519 signed offline license envelope validation, signed license import API, SQL-backed active license validation persistence, connector-count, sandbox-concurrency, audit-retention, solution-pack-install, SSO-provider-config, and SCIM-provider/import runtime enforcement, entitlement decisions, license status/import audit events, required install checks, rollback boundaries, offline transfer constraints, provider version history, provider change approvals, model policy change approvals, SQL-backed provider rate-limit samples, and model policy version history. Ingress/TLS rollout, cloud-managed service overlays, live cluster startup verification, customer-specific install probes, customer restore environment orchestration, full portal/admin frontend packaging, and customer-specific compatibility expansion remain planned later. |
| CREAO-consistent client portal | `apps/web/`, `tests/web/test_workspace_frontend_contract.py`, `2026-07-01-01-product-logic.md`, `2026-07-01-08-client-portal-creao-ui.md`, `2026-07-01-26-mvp-cloud-poc-execution.md`, `review-decisions.md` | Covered and partially implemented | Minimal static workspace exists for chat, recent run history, run trace evidence, runtime state snapshot, run timeline, sandbox terminal output, artifacts, approval controls, and Bearer login/logout. Chat column, composer hint, Enter/Shift+Enter behavior, selector shape, auth controls, run history loading/selection through `/api/runs`, trace summary loading through `/api/runs/{run_id}/trace`, runtime-state summary loading through `/api/runs/{run_id}/state`, storage-backed artifact text previews/downloads, storage-backed browser-capture preview/download, safe sandbox terminal event rendering, and run polling until terminal status are tested. Full portal, SSO/MFA user flows, admin console, skill marketplace, and browser visual tests remain planned. |
| API/control plane | `2026-07-01-02-technical-architecture.md`, `2026-07-01-14-api-sdk-contracts.md`, `2026-07-01-26-mvp-cloud-poc-execution.md`, `docs/contracts/run-event-stream-contract.md`, `apps/api/src/taroai/app.py`, `tests/api/test_app.py`, `tests/api/test_auth_session_contract.py`, `tests/api/test_tenant_readiness.py` | Covered and partially implemented | Routes, streaming, OpenAPI, errors, idempotency, SDK shape, and versioning are planned; run lifecycle can use the SQL control-plane store through settings, runtime-state reads are exposed through `GET /api/runs/{run_id}/state`, run events carry per-run sequence values with SSE `id:` and reconnect replay through `after_sequence`/`Last-Event-ID`, cancellation is exposed through `POST /api/runs/{run_id}/cancel`, retry is exposed through `POST /api/runs/{run_id}/retry`, Bearer auth can replace dev headers, and current tenant readiness is exposed through authenticated context. |
| Unified API errors | `2026-07-01-14-api-sdk-contracts.md`, `2026-07-01-26-mvp-cloud-poc-execution.md`, current `apps/api/src/taroai/api/errors.py` | Covered and partially implemented | Current code has `ApiExceptionManager` and `ApiExceptionRule`. |
| OpenAI-compatible model flow | `apps/api/src/taroai/model_gateway/`, `apps/api/src/taroai/agent/runtime.py`, `2026-07-01-02-technical-architecture.md`, `2026-07-01-17-model-gateway-provider-governance.md`, `2026-07-01-26-mvp-cloud-poc-execution.md`, `review-decisions.md` | Covered and partially implemented | Product/MVP flow uses the Model Gateway boundary; runtime planning usage metering, safe model-plan audit records, run/tenant/workspace/user/agent model call/token budget guards with optional Settings-managed rolling budget windows, first-pass global and tenant/workspace-scoped allowed/denied model policy, scoped default models, policy-resolved model propagation into provider requests, model sensitivity limits, model API-key secret-ref lease resolution, API/SQL-managed policy and provider stores, staged model policy change-request approvals, model policy version history, Pydantic provider registry settings with provider-level `chat_request_options`, direct `TAROAI_MODEL_GATEWAY_CHAT_REQUEST_OPTIONS`, safe provider listing plus write/enable/disable/credential-rotation/version-list/rollback APIs, staged provider change-request approvals, API/worker router wiring, typed provider fallback policy for response errors and rate-limit skips, safe provider fallback attempt summaries, first-pass process-local provider request/token-per-minute limits, SQL-backed tenant/provider rate-limit samples shared by API and worker router instances, Redis-backed request reservations, Redis-backed `max_output_tokens` token reservations, provider IDs on model meters, `/readyz` configuration readiness with missing item labels and provider counts, standalone OpenAI-compatible provider verifier with strict JSON plan parsing and DeepSeek JSON-output options, provider-compatible payload construction that omits `tool_choice` unless tool schemas are declared, safe policy/provider-admin audit records, safe policy-denial audit records, safe gateway-failure audit records, and model request/response guardrail enforcement are started. Broader distributed budget governance remains planned. |
| Billing, audit, observability | `2026-07-01-07-billing-audit-observability.md`, `2026-07-01-10-security-compliance.md`, `2026-07-01-22-incident-slo-support-operations.md`, `2026-07-01-26-mvp-cloud-poc-execution.md`, `apps/api/src/taroai/app.py`, `apps/api/src/taroai/audit/`, `apps/api/src/taroai/billing/`, `apps/api/src/taroai/embeddings/`, `apps/api/src/taroai/observability/`, `apps/api/src/taroai/incidents/`, `tests/api/test_app.py`, `tests/api/test_audit.py`, `tests/api/test_agent_runtime_context.py`, `tests/api/test_billing_pricing.py`, `tests/api/test_billing_pricing_repository.py`, `tests/api/test_billing_invoice_repository.py`, `tests/api/test_incident_models.py`, `tests/api/test_slo_error_budget.py` | Covered and partially implemented | Meter and audit events are recorded in the control-plane store, API audit writes/list reads plus request actor attribution, first-pass global/tenant/workspace billing pricing rules calculate `cost_estimate` for model and embedding meters without hard-coded runtime prices, tenant-scoped pricing rule read/manage APIs plus in-memory/SQL-compatible pricing rule stores feed API and worker runtime pricing, first-pass invoice views and persisted invoice snapshots group period-filtered priced and unpriced tenant meter usage, Agent Runtime model/tool/embedding audits with actor attribution, run-scoped and operation-level embedding usage meters, Tool Gateway service-level blocked/approval-required audits with actor attribution, worker job lifecycle audits with actor attribution, tenant bootstrap completion audits, identity/RBAC user-role lifecycle audits, billing meter audits, run-cancellation/retry audits, approval-resolution/rejection audits, skill-publication audits, tenant coverage reports, settings-based audit retention metadata, and license-enforced audit retention limits route through first-pass `AuditService`; tenant-wide read APIs require `billing.read`/`audit.read`, pricing and invoice snapshot writes require `billing.manage`, coverage reports require `audit.read`, first-pass query filters are implemented, grouped billing summaries are started, first-pass run trace aggregation/export with `TraceSpan`/`TraceEvent` entries, runtime stage spans, sanitized guardrail findings, HTTP exporter boundary, and error classification is started, first-pass incident Pydantic models plus in-memory incident lifecycle service are started, and first-pass SLO/error-budget target models plus in-memory status calculation are started. Tiered pricing, discounts, tax/credit adjustments, invoice status workflow, external finance export, pricing-rule versioning/approval workflow, live collector deployment verification, remaining audit matrix event producers, bootstrap/identity/system actor attribution, pagination, incident API/linkage, live SLO metric ingestion, and incident persistence remain planned. |
| Short-term and long-term memory | `2026-07-01-03-storage-identity-memory.md`, `2026-07-01-04-knowledge-rag-memory.md`, `2026-07-01-12-self-evolving-evaluations.md` | Covered | Redis TTL short-term memory and reviewed long-term memory are separated. |
| Storage choices | `2026-07-01-03-storage-identity-memory.md`, `2026-07-01-20-data-lifecycle-backup-recovery.md`, `2026-07-01-24-private-deployment-packaging.md`, `apps/api/src/taroai/storage/repository.py`, `tests/api/test_storage_repository.py` | Covered and partially implemented | PostgreSQL, Redis, object storage, vector backend, restore, and retention are planned; SQLite-compatible storage metadata persistence, first-pass object ACL/sensitivity enforcement, configurable upload content scanning, retention cleanup, storage-object export manifests/bundles, and backup manifest contracts are started. |
| User accounts, passwords, roles, permissions | `2026-07-01-03-storage-identity-memory.md`, `2026-07-01-10-security-compliance.md`, `2026-07-01-13-enterprise-onboarding.md`, `apps/api/src/taroai/identity/`, `apps/api/src/taroai/auth/`, `apps/api/src/taroai/onboarding/`, `apps/api/src/taroai/policy/`, `apps/api/src/taroai/sso/`, `apps/api/src/taroai/scim/`, `tests/api/test_auth_session_contract.py`, `tests/api/test_identity_repository.py`, `tests/api/test_sso_providers.py`, `tests/api/test_scim_provisioning.py`, `tests/api/test_tenant_readiness.py`, `tests/api/test_policy.py`, `tests/api/test_audit.py` | Covered and partially implemented | Password hashes, RBAC, disabled-user rejection, SQLite-compatible SQL identity persistence, signed PoC access tokens with session IDs, `/api/auth/logout`, SQL-backed session revocation, dev-header fallback setting, token-protected first-owner bootstrap, tenant readiness checks, centralized first-pass policy decisions, user/role create/assign/disable audit events, tenant-scoped SSO provider configuration APIs with SQL/RLS persistence plus entitlement checks, AuthService password-login rejection for SSO-only domains, and SCIM provider/import/group-role mapping APIs with external user links, SQL/RLS persistence, entitlement checks, and safe audit events are started. Production PostgreSQL rollout, OIDC/SAML login, full SCIM v2 service-provider compatibility, MFA, ABAC, risk/sensitivity rules, and support-access controls remain planned. |
| Multi-tenant isolation | `2026-07-01-02-technical-architecture.md`, `2026-07-01-03-storage-identity-memory.md`, `2026-07-01-10-security-compliance.md` | Covered and partially implemented | Tenant/workspace/user columns, service checks, PostgreSQL tenant session context setting before tenant-scoped SQL, PostgreSQL-only RLS migration blocks for tenant-scoped tables, and live PostgreSQL RLS verification against a non-superuser app role are started. CI/private-deployment release gates remain planned. |
| Knowledge base sharing and ACL-aware retrieval | `apps/api/src/taroai/knowledge/`, `apps/api/src/taroai/app.py`, `apps/api/src/taroai/embeddings/`, `tests/api/test_knowledge.py`, `tests/api/test_knowledge_repository.py`, `tests/api/test_embedding_gateway.py`, `tests/api/test_agent_runtime_context.py`, `2026-07-01-04-knowledge-rag-memory.md`, `2026-07-01-15-enterprise-connectors.md`, `2026-07-01-20-data-lifecycle-backup-recovery.md` | Covered and partially implemented | In-memory and SQLite-compatible SQL document/chunk persistence, managed source-content object upload with `storage_object_id`, configurable automatic source-content chunking when explicit chunks are omitted, OpenAI-compatible Embedding Gateway with secret-ref credential resolution, chunk embedding metadata persistence, safe embedding usage audit metadata without raw text or vectors, operation-level embedding meters for standalone knowledge APIs, run-scoped embedding meters during Agent Runtime retrieval, query-time ACL/sensitivity filtering, vector scoring when embeddings are available, citations, API endpoints behind `knowledge.write`/`knowledge.read`, safe audit events for base/document/query operations, runtime context loading, retrieval-context guardrail filtering before model planning, and connector sync worker ingestion into knowledge are started. Durable vector backend, richer parser policy, and advanced context policy remain planned. |
| Skill marketplace and custom enterprise skills | `2026-07-01-05-skills-tool-gateway.md`, `2026-07-01-08-client-portal-creao-ui.md`, `2026-07-01-19-agent-builder-workflow-templates.md`, `2026-07-01-23-solution-packs-customer-success.md`, `apps/api/src/taroai/skills/`, `apps/api/src/taroai/solution_packs/`, `apps/api/src/taroai/app.py`, `apps/api/migrations/001_initial.sql`, `tests/api/test_skill_api.py`, `tests/api/test_skill_repository.py`, `tests/api/test_solution_packs.py` | Covered and partially implemented | Tenant-scoped skill register/list/get/publish/disable API, tenant/department/workspace/private visibility filtering, version history lookup API, marketplace analytics API, workspace install/list/enable/disable API, dev-mode workspace skill invocation through Tool Gateway with manifest scope enforcement, safe audit metadata, `skill_call_count` billing, and SQLite-compatible SQL persistence are started behind identity permissions. Solution pack manifests, memory/SQL pack registry, pack version history, pack install records, install API, `solution_packs` license enforcement, safe install audit metadata, and skill-bundle installation into selected workspaces are started. Industry pack content, dry-run/rollback, adoption metrics, and feedback loops remain planned. |
| Governed Tool Gateway | `apps/api/src/taroai/tool_gateway/`, `apps/api/src/taroai/sandbox/tools.py`, `apps/api/src/taroai/guardrails/`, `apps/api/src/taroai/agent/runtime.py`, `apps/api/src/taroai/store.py`, `apps/api/src/taroai/db/repository.py`, `apps/api/src/taroai/api/errors.py`, `tests/api/test_tool_gateway.py`, `tests/api/test_agent_runtime.py`, `tests/api/test_db_repository.py`, `tests/api/test_sandbox.py`, `2026-07-01-05-skills-tool-gateway.md`, `2026-07-01-10-security-compliance.md`, `2026-07-01-15-enterprise-connectors.md` | Covered and partially implemented | Runtime invokes tools through the Tool Gateway boundary with context, PolicyService-derived granted scopes for tool-required permissions, runtime-created `session_id` injection for `sandbox.command` and `browser.action`, scope checks, approval decisions, input/output schema validation, short-lived secret lease injection, guardrail block/approval/redaction enforcement before handler execution, unified errors, service-level blocked/approval-required audit recording through injected `AuditService` with sensitive input redaction, gateway-policy approval pause, successful-call billing meters, skill-specific `skill_call_count` meters with skill-scoped pricing, successful-call audit events through injected `AuditService`, failed-call audit events with sensitive input redaction, and API/worker default runtime construction wires sandbox command and browser action handlers with a guardrail service. Connector integrations are started; arbitrary file discovery, secure sandbox providers, and browser live-view frontend remain planned. |
| Enterprise connectors | `2026-07-01-15-enterprise-connectors.md`, `apps/api/src/taroai/connectors/`, `apps/api/src/taroai/secrets/`, `apps/api/src/taroai/app.py`, `apps/api/migrations/001_initial.sql`, `apps/api/migrations/009_connector_definitions.sql`, `apps/api/migrations/010_connector_sync_state.sql`, `tests/api/test_connector_models.py`, `tests/api/test_connector_repository.py`, `tests/api/test_connector_api.py`, `tests/api/test_connector_sync_acl.py`, `tests/api/test_connector_tool_gateway_integration.py`, `tests/api/test_connector_dispatch.py`, `tests/api/test_connector_oauth.py`, `tests/api/test_secrets_boundary.py` | Covered and partially implemented | Pydantic connector models, credential-reference-only auth metadata, tenant/workspace-scoped memory and SQL registries, create/list/get/update/enable/disable APIs, safe registration and management audit metadata, migration/RLS coverage, persisted sync state/cursor records, sync ACL planning into knowledge documents, `connectors.sync` job enqueue/worker execution through a dedicated `connector_sync` worker kind with safe audit plus `connector_sync_document_count` billing, run-scoped capability invocation decisions with safe audit plus `connector_invocation_count` billing, approval-required invocation linkage to persisted run approvals with approved execution gating, internal API HTTP dispatch with method/path allowlists plus API-key/OAuth2 bearer access-token injection through short-lived secret leases, read-only database connector dispatch with secret-referenced DSNs, SELECT-only enforcement, table allowlists, OAuth authorize/callback/refresh management with token rotation through secret references, and AWS Secrets Manager value storage through the same lease API are started. SaaS/file/MCP provider adapters, provider-specific OAuth edge cases, broader database dialect/query governance, tenant-specific KMS/IAM policy hardening, and additional secret backend providers remain planned. |
| Secrets boundary | `2026-07-01-10-security-compliance.md`, `apps/api/src/taroai/secrets/`, `apps/api/src/taroai/tool_gateway/`, `apps/api/src/taroai/model_gateway/`, `apps/api/src/taroai/workers/runner.py`, `tests/api/test_secrets_boundary.py`, `tests/api/test_model_gateway_credentials.py`, `tests/api/test_settings.py`, `tests/api/test_tool_gateway.py`, `tests/api/test_backend_architecture_contract.py` | Covered and partially implemented | Pydantic secret references, scopes, short-lived leases, expiry checks, Tool Gateway lease injection with run/step/session context, Model Gateway API-key lease resolution with tenant/workspace/run binding, sandbox command lease-handle environment delivery with reserved-key override protection, sandbox-scoped lease resolution through `SecretService`, optional `X-Sandbox-Resolver-Token` enforcement through Pydantic Settings, AWS Secrets Manager value storage, Settings-driven API/worker backend selection, and safe audit metadata are started. Sandbox provider network isolation and mTLS/IAM hardening, tenant-specific KMS/IAM policy hardening, additional secret backend providers, and secret-manager live verification remain planned. |
| Agent loop and long-running runs | `apps/api/src/taroai/agent/runtime.py`, `apps/api/src/taroai/store.py`, `apps/api/src/taroai/guardrails/`, `tests/api/test_agent_runtime.py`, `tests/api/test_agent_runtime_context.py`, `tests/api/test_guardrail_policy_models.py`, `tests/api/test_tool_gateway.py`, `2026-07-01-02-technical-architecture.md`, `2026-07-01-06-agent-runtime-sandbox.md`, `2026-07-01-17-model-gateway-provider-governance.md`, `2026-07-01-21-prompt-guardrail-governance.md` | Covered and partially implemented | Runtime state snapshots, runtime-state read API, run cancellation, run retry, approvals, approval rejection, retries, model gateway, PolicyService-derived tool scopes, first-pass runtime sandbox and browser session creation/session-id injection, declared `/workspace/artifacts/**` sandbox artifact promotion or automatic `/workspace/artifacts/**` discovery with artifact-stage guardrail content evaluation and storage content scanning before upload, rejection of declared artifact paths outside `/workspace/artifacts/`, success/failure/approval-rejection/cancellation cleanup for runtime-created sandbox sessions, first-pass guardrail policy models, Tool Gateway request guardrail enforcement, retrieval-context guardrail enforcement, model request/response guardrail enforcement, persisted model-planning guardrail approval resume, artifact publication guardrail enforcement with persisted approval resume, and memory-write guardrail enforcement are started; snapshot policy and more durable repository hardening remain planned. |
| Multi-agent behavior | `2026-07-01-01-product-logic.md`, `2026-07-01-06-agent-runtime-sandbox.md`, `2026-07-01-18-triggers-scheduling-automation.md` | Covered | Bounded delegation is planned; unbounded swarm behavior is explicitly out. |
| Triggers and scheduling automation | `2026-07-01-18-triggers-scheduling-automation.md`, `apps/api/src/taroai/triggers/`, `apps/api/src/taroai/workers/scheduler_worker.py`, `apps/api/src/taroai/workers/trigger_worker.py`, `apps/api/src/taroai/app.py`, `infra/k8s/worker.yaml`, `docs/operations/triggers-runbook.md`, `tests/api/test_trigger_models.py`, `tests/api/test_schedule_evaluation.py`, `tests/api/test_trigger_scheduler_worker.py`, `tests/api/test_trigger_repository.py`, `tests/api/test_worker_runner.py`, `tests/api/test_worker_deployment_contract.py`, `tests/api/test_trigger_admin_api.py`, `tests/api/test_trigger_webhook.py`, `tests/api/test_connector_event_triggers.py`, `tests/api/test_agent_handoff_triggers.py`, `tests/api/test_trigger_operations.py` | Covered and partially implemented | Trigger models, schedule config/evaluation with formal `triggers.due` job payloads, connector-event config/matching, handoff config/execution, scheduler queue insertion, due-job run creation, SQL trigger persistence, durable scheduler and due-worker deployment wiring, Settings-managed signed webhook verification, webhook delivery-id idempotency, accountable context validation, create/list/get/enable/disable/invoke/webhook/connector-events/agent-handoff APIs, operations status API, autonomous run creation, safe invocation audit metadata, and trigger invocation billing are started. Provider-specific connector adapters/ACL mapping, advanced handoff approval policy, alert integration, automated remediation, and tenant/workspace-level signing secret rotation remain planned. |
| Sandbox / virtual employee environment | `apps/api/src/taroai/sandbox/`, `apps/api/src/taroai/app.py`, `apps/api/src/taroai/workers/runner.py`, `tests/api/test_sandbox.py`, `tests/api/test_sandbox_docker.py`, `tests/api/test_sandbox_local_process.py`, `tests/api/test_worker_runner.py`, `tests/api/test_agent_runtime.py`, `tests/api/sandbox_adapters.py`, `tests/api/test_playwright_browser_service.py`, `2026-07-01-06-agent-runtime-sandbox.md`, `2026-07-01-09-deployment-operations.md`, `2026-07-01-24-private-deployment-packaging.md` | Covered and partially implemented | Sandbox session/command/file/snapshot/browser models, disabled default sandbox/browser provider boundaries, explicit `local_process` provider for local cloud PoC command execution inside per-session workspaces, first-pass Docker provider selection through Pydantic settings and the shared factory path, Docker `--network none` container creation, Settings-managed memory/CPU/pids limits, non-root `--user`, read-only rootfs, `cap-drop=ALL`, `security-opt`, tmpfs mounts, workspace bind mounts, `docker exec` command execution, file upload/download/list/snapshot, destroy lifecycle, and rejection of unmanaged open/allowlist networking, first-pass Playwright HTTP browser controller service with session/action/screenshot contract and local Compose wiring, API endpoints for session create, command execution, file upload/download, snapshot, destroy, and browser actions, Tool Gateway sandbox command and browser action handlers wired into default API and worker runtime construction through provider contracts, worker browser startup uses the same Settings-backed HTTP controller for `playwright`/`browserbase`, runtime `sandbox.command` and `browser.action` steps can create sessions, inject `session_id`, execute through Tool Gateway, persist sandbox/browser session IDs in runtime state snapshots, promote declared `artifact_paths` files only under `/workspace/artifacts/` or auto-discovered `/workspace/artifacts/**` files into storage-backed artifacts after artifact-stage guardrail content evaluation and storage content scanning, reject declared artifact paths outside `/workspace/artifacts/`, emit safe `tool_call.completed`, `sandbox.command.executed`, `sandbox.artifacts.discovered`, `sandbox.artifact.promoted`, `sandbox.artifact.rejected`, and `browser.action.performed` events without raw sandbox stdout/stderr or browser text plus store-level `artifact.created`, and destroy runtime-created sandbox sessions on success, failure, approval rejection, or cancellation, sandbox command lease-handle environment delivery without raw secret values, sandbox-scoped lease resolution API with run/step/session validation, optional provider token enforcement, and safe audit metadata, sandbox/browser API permission checks, session/command/file/snapshot/destroy/browser audit metadata, sandbox command output object upload, sandbox file object upload, sandbox snapshot JSON object upload, browser screenshot object upload, command `sandbox_minutes` metering, file-upload `artifact_bytes` metering, and browser action `browser_action_count` metering are started. Local contract adapters stay under `tests/` and are never runtime defaults. Kubernetes/E2B/microVM-backed isolation, browser live-view frontend, and final provider choice remain planned. |
| Sharing and collaboration | `2026-07-01-16-sharing-collaboration-artifacts.md`, `2026-07-01-08-client-portal-creao-ui.md`, `2026-07-01-20-data-lifecycle-backup-recovery.md`, `apps/api/src/taroai/sharing/`, `apps/api/migrations/021_share_grants.sql`, `tests/api/test_share_grants.py`, `tests/api/test_share_grant_repository.py`, `tests/api/test_app.py` | Covered and partially implemented | First-pass typed share grants, create/list/revoke APIs, safe audit metadata, memory/SQL persistence, settings, migration, RLS, direct artifact share-grant authorization for storage signed-url/content read paths, and default-off external artifact link download with artifact-only and view-only external-link grants, 32-character minimum link-token input, tenant-scoped keyed `hmac-sha256:` subject storage, and token-redacted API response/audit plus byte-meter metadata are started. Artifact visibility states, password-protected or separately signed external links, collaboration, promotion, promotion/evaluation billing, and retention automation remain planned. |
| Self-evolving behavior | `2026-07-01-01-product-logic.md`, `2026-07-01-12-self-evolving-evaluations.md`, `2026-07-01-21-prompt-guardrail-governance.md` | Covered with safety boundary | Only reviewed candidates, evals, versioned publication, and rollback are allowed. |
| Enterprise onboarding | `2026-07-01-13-enterprise-onboarding.md`, `2026-07-01-23-solution-packs-customer-success.md`, `2026-07-01-26-mvp-cloud-poc-execution.md`, `apps/api/src/taroai/onboarding/`, `tests/api/test_tenant_readiness.py` | Covered and partially implemented | Token-protected first-owner bootstrap, slug-derived idempotent starter workspace/knowledge/skill-pack seed, and readiness checks are started. Full onboarding orchestration, selectable industry packs, quotas enforcement, invite workflow, and rollout runbooks remain planned. |
| `.env` and Pydantic backend management | `apps/api/src/taroai/config.py`, `2026-07-01-03-storage-identity-memory.md`, `2026-07-01-11-testing-release-quality.md`, `review-decisions.md` | Covered and partially implemented | Current config uses Pydantic Settings and `.env`; plans require Pydantic at service boundaries. |
| No `from __future__ import annotations` | `tests/api/test_backend_style_contract.py`, `review-decisions.md`, plan verification gates | Covered and enforced | Evidence gate checks backend source. |
| Maintainable package boundaries | `apps/api/src/taroai/agent`, `memory`, `skills`, `storage`, `identity`; `2026-07-01-02-technical-architecture.md` | Covered and partially implemented | Current backend is split by bounded context; future packages are planned. |

## Current-Code Facts

Current backend foundations exist for:

- FastAPI app and run APIs.
- Pydantic settings with `.env` support, including first-pass data residency region settings.
- Centralized API exception management.
- In-memory store, runtime, runtime state snapshots, runtime-state read API, run retry, run cancellation with pending approval cancellation, SQLite-compatible SQL control-plane repository wired through settings, memory services with Redis-backed short-term put/get/list/delete/tenant-delete, SQLite-compatible SQL long-term memory persistence and tenant redaction, candidate review, SQL-backed short-term memory review queue APIs, memory APIs, runtime context loading, and audit metadata emission through settings, knowledge service with SQLite-compatible SQL metadata/chunk persistence, OpenAI-compatible Embedding Gateway usage audit plus run-scoped and operation-level retrieval meters, source-content object upload, and tenant-scoped metadata/chunk deletion through settings, tenant-scoped skill registry/API with workspace installation records and SQLite-compatible SQL persistence through settings, identity service, SQLite-compatible SQL identity service wired through settings, signed PoC auth/session service with logout and SQL-backed revocation, first-pass Policy Service with centralized RBAC-backed decisions for API tenant operations, first-pass AuditService with sensitive metadata redaction and settings-based retention metadata, FastAPI business audit write/list routing with request actor attribution, Agent Runtime model/tool/embedding audit routing with actor attribution, Tool Gateway service-level blocked/approval-required audit routing with actor attribution, tenant bootstrap completion audit routing, identity user/role audit routing, billing meter audit routing, run-cancellation/retry audit routing, approval-resolution/rejection audit routing, skill-publication audit routing, and `GET /api/audit-events/coverage` default enterprise audit coverage reports, first-pass secrets boundary with scoped short-lived leases, Tool Gateway lease injection, AWS Secrets Manager value storage selected through settings, and safe audit metadata, token-protected first-owner bootstrap API with slug-derived starter workspace/knowledge/skill-pack seed, tenant readiness service/endpoint, tenant-wide billing/audit read permission checks, first-pass filters, grouped billing summaries, and first-pass run trace aggregation with `TraceSpan`/`TraceEvent` entries plus error classification, storage catalog, SQLite-compatible SQL storage metadata catalog wired through settings, S3/MinIO-compatible object storage adapter boundary, storage metadata/signed URL/content upload/content download API endpoints behind `storage.read`/`storage.write`, tenant/workspace-scoped internal storage objects, retention-aware object delete endpoint, first-pass expired-object cleanup lifecycle service with active legal-hold skip checks and preview mode, Pydantic lifecycle policy/legal-hold/export/backup/data-residency/offboarding stores and services with tenant default plus workspace override resolution, offboarding approval state advancement, approved tenant-scoped export bundle execution, first-pass storage-object deletion execution, first-pass memory tombstone/delete execution, and first-pass knowledge metadata/chunk deletion execution, sandbox command output object upload, sandbox file object upload, sandbox snapshot JSON object upload, browser screenshot object upload, storage lifecycle columns, `storage_bytes` meter recording, `storage.uploaded` audit metadata, `storage.downloaded` audit metadata, `storage.signed_url.created` audit metadata, and `storage.deleted` audit metadata.
- OpenAI-compatible Model Gateway Pydantic models, configuration fields, runtime planning boundary, planning usage meters, safe model-plan audit metadata, first-pass global and tenant/workspace-scoped allowed/denied model policy with scoped default models, policy-resolved model propagation into provider requests, in-memory and SQLite-compatible SQL model policy/provider stores with API management, Pydantic provider registry settings, safe provider listing plus write/enable/disable/credential-rotation APIs, API/worker router wiring, typed provider fallback policy for response errors and rate-limit skips, first-pass process-local provider request/token-per-minute limits, provider IDs on model meters, safe policy/provider-admin audit metadata, safe policy-denial audit metadata, safe gateway-failure audit metadata, run/tenant/workspace/user/agent model budget guard, and model request/response guardrail enforcement with summary-only audit metadata.
- Connector package with Pydantic connector type/status/auth/capability/definition models, credential-reference-only auth metadata, tenant/workspace-scoped memory and SQLite/PostgreSQL-compatible SQL registries, FastAPI create/list/get/update/enable/disable APIs behind connector permissions, safe `connector.registered`, `connector.updated`, `connector.enabled`, and `connector.disabled` audit metadata, migration/RLS coverage, persisted sync state/cursor records, sync ACL planning into knowledge document creation without direct long-term memory writes, `connectors.sync` job enqueue and worker execution through a dedicated `connector_sync` worker kind into knowledge with safe audit plus `connector_sync_document_count` billing, run-scoped connector capability invocation decisions behind `connectors.invoke` with safe audit metadata plus `connector_invocation_count` billing for ready decisions, approval-required connector invocation linkage to persisted run approval requests with approved execution gating, internal API HTTP dispatch with method/path allowlists plus API-key/OAuth2 bearer access-token injection through short-lived secret leases, read-only database connector dispatch with secret-referenced DSNs, SELECT-only enforcement, table allowlists, safe failure audit, OAuth authorize/callback/refresh management with token rotation through secret references, and AWS Secrets Manager value storage through the same lease API.
- Tool Gateway Pydantic models, registered-handler service, scope/approval policy checks, input/output schema validation, optional blocked-call audit recording, guardrail block/approval/redaction enforcement before handler execution, runtime policy-approval pause, successful-call audit/billing recording, failed-call audit redaction, and unified API error mapping.
- Guardrails Pydantic models and tenant/workspace-scoped in-memory rule evaluation for allow, warn, redact, approval-required, block, and quarantine-run decisions, Settings-backed built-in secret-pattern, prompt-threat, and HTTP detector boundaries, plus default API/worker guardrail wiring, Agent Runtime retrieval-context filtering, model request/response filtering, artifact metadata and sandbox artifact content publication filtering with persisted approval resume, long-term memory candidate guardrail approval review, SQL-backed short-term memory review queue approval/rejection, and long-term/short-term memory-write filtering with summary-only audit records.
- Knowledge Pydantic models, in-memory registration/retrieval, SQLite-compatible metadata/chunk persistence, tenant-scoped metadata/chunk deletion, source-content object upload with `storage_object_id`, OpenAI-compatible Embedding Gateway query vectors, safe embedding usage audit metadata, operation-level and run-scoped embedding retrieval meters, ACL-aware query filtering, citations, API endpoints behind `knowledge.write`/`knowledge.read`, runtime retrieval-context guardrail filtering, and safe knowledge audit metadata.
- Sandbox and browser Pydantic models, disabled default sandbox/browser provider boundaries, explicit `local_process` provider for local cloud PoC command execution inside per-session workspaces, first-pass Settings-hardened Docker disabled-network container provider, Tool Gateway sandbox command and browser action handlers wired into `create_app` default runtime through provider contracts, runtime sandbox command session creation/session-id injection plus declared `/workspace/artifacts/**` artifact path promotion or auto-discovered `/workspace/artifacts/**` promotion after artifact-stage guardrail content evaluation and storage content scanning, rejection of declared artifact paths outside `/workspace/artifacts/`, and safe tool completion summaries without raw stdout/stderr/browser text, success/failure/approval-rejection/cancellation cleanup for runtime-created sessions, sandbox command lease-handle environment delivery without raw secret values, sandbox-scoped lease resolution API with run/step/session validation, optional provider token enforcement, and safe audit metadata, API endpoints for session create, command execution, file upload/download, snapshot, destroy, and browser actions, sandbox/browser API permission checks, session/command/file/snapshot/destroy/browser audit metadata, sandbox command output object upload, sandbox file object upload, sandbox snapshot JSON object upload, browser screenshot object upload, command `sandbox_minutes` metering, file-upload `artifact_bytes` metering, and browser action `browser_action_count` metering. Local contract adapters stay under `tests/` and are injected only by tests.
- SQL migration contract with runtime state table, migration runner, and SQL control-plane repository tests for run/event/status/artifact/approval/meter/audit/runtime-state persistence.
- Worker/queue Pydantic job contracts for run execution, billing aggregation, cleanup, connector sync, trigger due, and trigger scheduler paths; queue claim/ack/fail/reject lifecycle tests; retry/dead-letter policy; Redis-backed queue adapter boundary with expired lease recovery for crashed workers; configurable API enqueue mode for run execution; worker job lifecycle audit events with actor attribution; Pydantic worker runner/entrypoint with settings-based SQL control-plane store, knowledge-service, and connector-registry selection plus default sandbox/browser Tool Gateway handlers; worker-kind CLI for agent, cleanup, connector-sync, trigger-due, and trigger-scheduler execution; cleanup worker runner for storage lifecycle cleanup; connector sync, trigger scheduler, and due worker runners; first-pass Kubernetes worker process manifests; and first-pass Kubernetes API/PostgreSQL/Redis/MinIO/NetworkPolicy manifests with runtime ConfigMap/Secret boundaries.
- Trigger automation Pydantic models and service boundary for schedule, webhook, API, connector event, and agent handoff trigger definitions; schedule config/evaluation with cron, timezone, start/end, and catch-up cap; `triggers.due` job payload emission without inline run execution; scheduler worker queue insertion with `next_run_at` advancement and safe schedule audit metadata; `TriggerDue` worker run creation plus `runs.execute` queue insertion; SQLite/PostgreSQL-compatible `SqlTriggerStore` with Settings/App/worker wiring and RLS migration coverage; tenant/workspace/accountable actor validation; disabled trigger invocation blocking; FastAPI create/list/get/enable/disable/invoke/webhook/connector-events/agent-handoff/operations endpoints behind trigger permissions; operations status classification from trigger state and audit events; autonomous run creation through the control-plane path; safe `trigger.invoked` audit metadata; and `trigger_invocation_count` billing meters.
- Pytest suite under `tests/api`.

Current backend does not yet provide:

- Production PostgreSQL adapter rollout, IdP/SCIM-backed storage subject mapping, multipart upload, production DLP/AV scanning adapters, non-storage lifecycle cleanup categories beyond memory, tenant-wide asynchronous export orchestration, broader offboarding deletion orchestration, backup execution, customer restore environment orchestration, physical multi-region residency enforcement, and Kubernetes hardening beyond the first-pass API/worker/backing-service/NetworkPolicy manifests.
- Production PostgreSQL migrations tooling and CI/private-deployment release gates for PostgreSQL migration/RLS verification.
- Runtime proposal wiring, richer memory review policy, and advanced context policy.
- Industry solution-pack content, packaged customer-success workflows, adoption health, and feedback loops.
- Full Knowledge/RAG integrations for durable vector backend, richer parser policy, and advanced context policy.
- SaaS/file/MCP connector adapters, provider-specific OAuth edge cases, broader database dialect/query governance, tenant-specific KMS/IAM policy hardening, and additional secret backend providers.
- Provider-specific guardrail integration policy and broader semantic threat coverage.
- Remaining audit matrix event producers, bootstrap/identity/system actor attribution, connector-specific read/write audit taxonomy, and richer sandbox execution.
- Kubernetes/E2B/microVM-backed sandbox isolation, production filesystem/network hardening, browser live-view frontend, and snapshot retention policy.
- Model Gateway broader distributed budget governance.
- Billing tiering/discounts/tax and credit adjustments/invoice status workflow/external finance export/pricing-rule versioning and approval workflow, live observability collector deployment verification, pagination, SLOs, and incident links.
- OIDC/SAML login, full SCIM v2 service-provider compatibility, MFA, production identity rollout, full onboarding orchestration, selectable starter packs, support-access controls, and readiness runbooks.
- Full frontend portal beyond the minimal static workspace: production SSO/MFA flows, advanced run history/search, live browser panel, rich artifact preview/download, admin console, skill marketplace, generated SDKs, and browser visual/accessibility tests.
- Ingress/TLS rollout, cloud-managed PostgreSQL/Redis/object-storage overlays, autoscaling production policy, customer-specific install probes, customer restore environment orchestration, customer-specific compatibility expansion, and live Kubernetes startup verification beyond the manifest/Helm/config/licensing/install-report/runbook contract tests, restore drill evidence builder, scheduled restore drill due-job scheduler plus SQL-backed due-worker request-record intake, release package signature verification, and existing PostgreSQL RLS, Redis worker queue, Redis short-term memory, and MinIO/S3-compatible object storage verifiers.
- Provider-specific connector adapters and ACL mapping, advanced handoff approval policy, tenant/workspace-level signing secret rotation policy, alert integration, and automated remediation.
- Workspace/starter-pack onboarding orchestration beyond the current general starter seed.

## Open Human Decisions

These block implementation approval, not plan usefulness:

| ID | Decision | Recommended Default |
| --- | --- | --- |
| Q-001 | First industry pack | General starter pack only. |
| Q-002 | First sandbox provider | `local_process` for local cloud PoC, `docker` for first-pass disabled-network container execution, and Kubernetes, E2B, or microVM-backed execution before shared enterprise use. |
| Q-003 | First vector backend | Internal retrieval contract first; pgvector for durable PoC if approved. |
| Q-004 | First model gateway strategy | OpenAI-compatible Model Gateway contract first. |
| Q-005 | MVP auth mode | Password PoC plus dev headers behind settings. |
| Q-006 | Frontend timing | Answered: implement a minimal static workspace for local PoC; defer the full portal. |
| Q-007 | Private deployment priority | Not sales-critical for MVP unless a named customer requires it. |
| Q-008 | API versioning timing | Keep `/api/*` for MVP; plan `/api/v1` before SDK release. |

## Review Verdict

The plan package is ready for human review. Foundational implementation has started, but the broader MVP milestone is not approved.

The next MVP milestone should start only after:

1. Q-001 through Q-005 and Q-008 are answered or explicitly deferred.
2. Plan 26 is marked `approved` or `approved with changes`.
3. Accepted answers are copied into `review-decisions.md`.
4. The evidence gate below passes.

## Evidence Gate

Run before claiming the plan package is review-ready:

```bash
find docs/plans -maxdepth 1 -type f -name '2026-07-01-*.md' | wc -l
rg -n "^# Review Readiness Audit|^## Requirement Coverage|^## Open Human Decisions" docs/plans/review-readiness-audit.md
rg -n "OpenAI-compatible Model Gateway" docs/plans/2026-07-01-02-technical-architecture.md docs/plans/2026-07-01-17-model-gateway-provider-governance.md docs/plans/2026-07-01-26-mvp-cloud-poc-execution.md
! rg -n "m[o]ck|M[o]ck|f[a]ke|F[a]ke|M[o]ckModelProvider" docs/plans
rg -n "from __future__ import annotations" apps/api/src
python -m pytest -q
```

Expected evidence:

- Dated plan count is `27`.
- This audit file exists and contains coverage and open-decision sections.
- Model flow is documented through OpenAI-compatible Model Gateway.
- Plans do not name prototype/test provider classes in product-flow docs.
- Backend source has no `from __future__ import annotations`.
- Existing tests pass.
