# Research Grounding Notes

This file records the source-backed terminology check for the Taroai enterprise Agent Workspace plans.

Use it before approving or expanding any plan that mentions external frameworks, protocols, sandbox providers, vector stores, observability, or frontend architecture.

## Method

1. Check primary or official sources first.
2. Inspect the current repo before writing implementation claims.
3. Separate `implemented now`, `planned boundary`, `candidate provider`, and `internal product term`.
4. If a term is vendor-specific or fast-moving, verify it again before implementation.

## Current Repo Facts

These are current-state facts from `/data/temp34/Taroai`:

| Area | Current State | Evidence |
| --- | --- | --- |
| API | FastAPI app exists with run, event stream, execute, approval, artifacts, run trace, trace export, billing meter, billing summary, and audit endpoints. Tenant-wide billing/audit/trace reads require `billing.read` or `audit.read`; billing/audit endpoints now support first-pass query filters through Pydantic query models, API business audit writes route through `AuditService`, and run trace includes first-pass `TraceSpan`/`TraceEvent` entries, runtime-derived stage spans, sanitized guardrail finding summaries, error classification, and an OTLP HTTP export boundary. | `apps/api/src/taroai/app.py`, `apps/api/src/taroai/audit/`, `apps/api/src/taroai/billing/`, `apps/api/src/taroai/observability/`, `tests/api/test_app.py`, `tests/api/test_audit.py` |
| Config | `.env` and environment settings are managed through Pydantic Settings with `TAROAI_` prefix. Data residency settings now include primary region, allowed regions, cross-region replication mode, vector index region, and sandbox provider region. | `apps/api/src/taroai/config.py`, `.env.example`, `tests/api/test_settings.py` |
| Errors | API errors are centralized through `ApiExceptionManager` and `ApiExceptionRule`. | `apps/api/src/taroai/api/errors.py` |
| Agent runtime | Runtime exists with approval pause/resume, retries, runtime state snapshots, retrieval-context guardrail filtering before model planning, model request/response guardrail filtering at the gateway boundary, and a planning boundary backed by the OpenAI-compatible Model Gateway contract. Approval resume can recover from stored runtime snapshots when process-local pending state is missing; SQL control-plane repository now persists approval requests, status changes, artifacts, and runtime snapshots through the same store method shape. Tests-only adapter fixtures live under tests, not product flow. | `apps/api/src/taroai/agent/`, `apps/api/src/taroai/model_gateway/`, `apps/api/src/taroai/store.py`, `apps/api/src/taroai/db/repository.py`, `tests/api/test_agent_runtime.py`, `tests/api/test_agent_runtime_context.py`, `tests/api/test_db_repository.py`, `tests/api/adapters.py` |
| Model Gateway | OpenAI-compatible request/response models and a default gateway boundary exist. Agent Runtime planning records `model_call_count`, `model_tokens_input`, `model_tokens_output`, and `model.plan.created` audit metadata when usage is returned, without storing prompt content in audit. A Pydantic budget guard can block exhausted run, tenant, workspace, user, and agent model call/token budgets before provider invocation and records `model.budget_exceeded` audit metadata. Pydantic Settings now provide first-pass global and tenant/workspace-scoped allowed/denied model lists plus scoped default models; an in-memory and SQLite-compatible SQL model policy store exposes `model_policy.read`/`model_policy.manage` API management, safe admin audit metadata, API runtime refresh, and worker startup loading. Policy rejections fail before provider invocation and emit safe `model.policy_denied` audit metadata. Runtime model request/response guardrails can block or redact content before provider invocation and before planned steps execute, while audit metadata keeps prompt/response text out. Gateway configuration/response failures now fail the run and emit safe `model.gateway_failed` audit metadata. Provider references, sensitivity constraints, budget windows, rate limits, model policy versioning/approval workflow, multi-provider routing, and production rollout decisions remain planned work. | `apps/api/src/taroai/model_gateway/`, `apps/api/src/taroai/agent/runtime.py`, `apps/api/src/taroai/app.py`, `apps/api/src/taroai/workers/runner.py`, `tests/api/test_agent_runtime.py`, `tests/api/test_settings.py`, `tests/api/test_model_policy.py`, `tests/api/test_model_policy_repository.py`, `tests/api/test_model_budget.py` |
| Tool Gateway | Pydantic request, policy, decision, risk, result, audit-record, and secret requirement models exist with a Tool Gateway service for registered handlers, scope checks, approval-required decisions, input/output schema validation, runtime context invocation, secret lease injection, unified API error mapping, service-level blocked/approval-required audit recording through injected `AuditService` with sensitive input redaction, and guardrail block/approval/redaction enforcement before handler execution. Runtime tool calls now pause when gateway policy or guardrail policy requires approval, emit `tool_call_count`/`tool.executed` records on success through injected `AuditService`, and emit `tool.failed` audit records with sensitive input redaction on failure. Connector integrations, skill-specific meters, and full sandbox execution remain planned work. | `apps/api/src/taroai/tool_gateway/`, `apps/api/src/taroai/agent/runtime.py`, `apps/api/src/taroai/store.py`, `apps/api/src/taroai/db/repository.py`, `apps/api/src/taroai/api/errors.py`, `tests/api/test_tool_gateway.py`, `tests/api/test_agent_runtime.py`, `tests/api/test_db_repository.py` |
| Secrets | Pydantic `SecretRef`, `SecretLease`, and `SecretScope` models exist with a first-pass in-memory service boundary for scoped short-lived leases. Long-lived values are kept outside model dumps, leases enforce tenant/workspace/tool/action/expiry constraints, Tool Gateway policies can request short-lived leases before handler execution, and audit metadata excludes secret values and lease tokens. External secret manager integration plus automatic Sandbox credential lease injection remain planned work. | `apps/api/src/taroai/secrets/`, `apps/api/src/taroai/tool_gateway/`, `tests/api/test_secrets_boundary.py`, `tests/api/test_tool_gateway.py`, `tests/api/test_backend_architecture_contract.py` |
| Knowledge | Pydantic knowledge base, document, chunk, retrieval request, and retrieval result models exist with in-memory and SQLite-compatible SQL knowledge metadata/chunk persistence, settings-based service selection, managed source-content object upload under `knowledge-documents`, `storage_object_id` linkage on knowledge documents, query-time tenant/workspace/ACL/sensitivity filtering, citation results, API endpoints for base creation/document registration/query, `knowledge.write`/`knowledge.read` API permission checks, safe audit events for base/document/query operations, and Agent Runtime context loading with sanitized `context.loaded` events. Runtime retrieval guardrails can remove or redact governed excerpts before model planning and record summary-only audit metadata. Query audit metadata records query length and result IDs, not raw query text or retrieved excerpts. Embeddings, vector backend, connector sync, advanced context policy, and LlamaIndex adapter remain planned work. | `apps/api/src/taroai/knowledge/`, `apps/api/src/taroai/agent/runtime.py`, `apps/api/src/taroai/app.py`, `apps/api/migrations/001_initial.sql`, `tests/api/test_knowledge.py`, `tests/api/test_knowledge_repository.py`, `tests/api/test_agent_runtime_context.py` |
| Sandbox and browser | Pydantic sandbox session, command, file, snapshot, and browser action/observation models exist with disabled default sandbox/browser provider boundaries, Tool Gateway `sandbox.command` and `browser.action` handler registration through provider contracts, sandbox API endpoints for session create, command execution, file upload/download, snapshot, and destroy, browser action API endpoint, `sandbox.create`/`sandbox.execute`/`browser.act` permission checks, session/command/file/snapshot/destroy/browser audit metadata, sandbox command output object upload, sandbox file object upload, sandbox snapshot JSON object upload, browser screenshot object upload with non-serialized screenshot bytes, `sandbox_minutes` command metering when a run exists, `artifact_bytes` file-upload metering when a run exists, and `browser_action_count` metering when a run exists. Local contract adapters stay under `tests/` and are injected only by tests; browser audit metadata records input text presence and length, not raw typed text. Real isolated execution provider, secure filesystem, live browser provider, sandbox-generated file discovery/artifact promotion, and E2B/K8s/provider adapters remain planned work. | `apps/api/src/taroai/sandbox/`, `apps/api/src/taroai/app.py`, `tests/api/test_sandbox.py`, `tests/api/sandbox_adapters.py` |
| Memory | Pydantic short-term TTL memory and long-term scoped memory services exist. Redis-backed short-term put/get/list/delete with TTL and tenant-scoped delete is available through `TAROAI_SHORT_TERM_MEMORY_BACKEND`; SQLite-compatible SQL long-term memory persistence and tenant-scoped redacted expiration are available through `TAROAI_LONG_TERM_MEMORY_BACKEND`. Long-term memory candidate creation, approve/reject review, active scoped reads, API endpoints, audit metadata emission, Agent Runtime context loading, and offboarding memory deletion are started. Live Redis deployment verification, runtime proposal wiring, advanced context policy, and richer review policy remain planned work. | `apps/api/src/taroai/memory/`, `apps/api/src/taroai/agent/runtime.py`, `apps/api/src/taroai/app.py`, `apps/api/migrations/001_initial.sql`, `tests/api/test_storage_identity_memory.py`, `tests/api/test_memory_repository.py`, `tests/api/test_memory_review.py`, `tests/api/test_agent_runtime_context.py`, `tests/api/test_app.py` |
| Skills | Pydantic skill manifest, tenant-scoped skill registry lifecycle, tenant/department/workspace/private visibility metadata and filtering, SQLite-compatible SQL skill registry persistence, skill version history persistence/lookup, workspace skill installation records, marketplace analytics, settings-based registry selection, and FastAPI endpoints for register/list/get/publish/disable plus version history, analytics, and workspace install/list/enable/disable exist behind identity permission checks. Production PostgreSQL rollout and connector-backed execution remain planned work. | `apps/api/src/taroai/skills/`, `apps/api/src/taroai/app.py`, `apps/api/migrations/001_initial.sql`, `tests/api/test_skills_memory.py`, `tests/api/test_skill_api.py`, `tests/api/test_skill_repository.py` |
| Storage | Pydantic tenant-scoped storage catalog exists with S3/MinIO-compatible object storage adapter boundary, upload contract, download contract, delete contract, signed URL contract, settings, FastAPI endpoints for metadata registration, run-scoped listing, signed URL creation, object content upload with declared-size validation, object content download, retention-aware object delete, tenant/workspace-scoped internal objects without `run_id`, knowledge document source object upload, sandbox command output object upload, sandbox file object upload, sandbox snapshot JSON object upload, and browser screenshot object upload, a SQLite-compatible SQL storage metadata catalog, lifecycle columns, object `acl_subjects`/`sensitivity_level`, read-side object ACL/sensitivity checks for signed URLs and content downloads, configurable upload content scanning through `TAROAI_OBJECT_STORAGE_CONTENT_SCAN_BLOCKED_TERMS`, storage lifecycle cleanup service with preview mode, and settings-based app wiring for the SQL catalog. Storage endpoints now enforce `storage.read`/`storage.write`; upload records `storage_bytes` billing meters and `storage.uploaded` audit metadata without persisting raw object content in audit; rejected uploads record `storage.content_rejected` metadata without raw content or rule text; download records `storage.downloaded` audit metadata without persisting raw object content in audit; signed URL creation records `storage.signed_url.created` audit metadata without persisting the generated URL; delete records a metadata tombstone and `storage.deleted` audit metadata. First-pass retention cleanup lists expired active storage objects by tenant/workspace, deletes through the object storage adapter, tombstones metadata, emits system `storage.deleted` audit metadata, skips deletion for active legal holds with `storage.retention_skipped` audit metadata, and can preview would-delete IDs without adapter delete or metadata tombstone. Production PostgreSQL rollout, live MinIO/S3 deployment verification, IdP/SCIM-backed subject mapping, multipart upload, production DLP/AV scanning adapters, non-storage cleanup categories, and broader read-side billing coverage remain planned work. | `apps/api/src/taroai/storage/`, `apps/api/src/taroai/app.py`, `apps/api/migrations/001_initial.sql`, `tests/api/test_storage_adapter.py`, `tests/api/test_storage_identity_memory.py`, `tests/api/test_storage_repository.py`, `tests/api/test_storage_lifecycle.py`, `tests/api/test_migration_contract.py`, `tests/api/test_app.py`, `tests/api/test_sandbox.py` |
| Data residency | Pydantic data residency reports exist for checking configured object storage, vector index, and enabled sandbox provider regions against allowed regions. The FastAPI report endpoint is behind `lifecycle.read` and emits summary-only audit metadata. Physical multi-region provisioning and provider-level enforcement remain planned work. | `apps/api/src/taroai/lifecycle/residency.py`, `apps/api/src/taroai/app.py`, `docs/security/data-residency.md`, `tests/api/test_data_residency_config.py`, `tests/api/test_lifecycle_api.py` |
| Identity/Auth/Readiness | Pydantic identity service exists with password hashing, role assignment, disabled-user handling, signed PoC access tokens, `/api/auth/login`, `/api/auth/logout`, Bearer request-context resolution, dev request headers behind settings, a SQLite-compatible SQL identity service for users/roles/role assignments, SQL-backed auth session persistence/revocation when SQL identity is used, token-protected `/api/tenants/bootstrap` for first-owner seed, tenant readiness checks exposed at `/api/tenants/current/readiness`, and user/role lifecycle audit events emitted through injected `AuditService` without password material. Full onboarding orchestration, starter packs, production PostgreSQL rollout, SSO/OIDC/SAML, SCIM, MFA, and support-access controls remain planned work. | `apps/api/src/taroai/identity/`, `apps/api/src/taroai/auth/`, `apps/api/src/taroai/onboarding/`, `apps/api/src/taroai/app.py`, `tests/api/test_auth_session_contract.py`, `tests/api/test_identity_repository.py`, `tests/api/test_tenant_readiness.py`, `tests/api/test_storage_identity_memory.py`, `tests/api/test_audit.py` |
| Policy | Pydantic policy request/decision/effect models exist with an `IdentityPolicyService` for first-pass RBAC-backed decisions. FastAPI tenant-operation permission checks route through `app.state.policy_service`. ABAC, risk scoring, sensitivity clearance, support access, policy reasons taxonomy, and approval escalation remain planned work. | `apps/api/src/taroai/policy/`, `apps/api/src/taroai/app.py`, `tests/api/test_policy.py`, `tests/api/test_backend_architecture_contract.py` |
| Guardrails | Pydantic guardrail stage, action, severity, rule, condition, evaluation request, redaction, detector finding, and decision models exist with tenant/workspace-scoped in-memory rule evaluation plus Settings-backed built-in secret-pattern, prompt-threat, and HTTP detector boundaries. Current decisions cover allow, warn, redact, approval-required, block, and quarantine-run outcomes. Tool Gateway request enforcement, Agent Runtime retrieval-context enforcement, model request/response enforcement, persisted model-planning guardrail approval resume, artifact publication enforcement with persisted approval resume, long-term memory candidate guardrail approval review, SQL-backed short-term memory review queue approval/rejection, long-term/short-term memory-write enforcement, and sanitized guardrail finding summaries in run traces are started, and default API plus worker runtime paths receive a guardrail service. Remaining guardrail work: provider-specific guardrail integration policy, broader semantic threat coverage, and production PostgreSQL hardening for review storage. | `apps/api/src/taroai/guardrails/`, `apps/api/src/taroai/tool_gateway/`, `apps/api/src/taroai/memory/`, `apps/api/src/taroai/agent/runtime.py`, `apps/api/src/taroai/app.py`, `apps/api/src/taroai/workers/runner.py`, `tests/api/test_guardrail_policy_models.py`, `tests/api/test_tool_gateway.py`, `tests/api/test_agent_runtime.py`, `tests/api/test_agent_runtime_context.py`, `tests/api/test_app.py`, `tests/api/test_worker_runner.py` |
| Audit | Pydantic audit create/actor/resource/action models and coverage matrix/report models exist with an `AuditService` boundary. The service records through the control-plane store, recursively redacts sensitive metadata keys, returns defensive copies, checks tenant coverage against default enterprise requirements, stores request actor attribution for FastAPI business audit writes, stores Agent Runtime, Tool Gateway, and worker job actor attribution from tenant/user context, and FastAPI business audit writes/list reads plus `GET /api/audit-events/coverage` route through `app.state.audit_service` behind `audit.read`. Agent Runtime model/tool audit events, Tool Gateway service-level blocked/approval-required events, worker job started/succeeded/failed events, tenant bootstrap completion audit events, identity user/role lifecycle events, billing meter events, approval-resolution events, and skill-publication events route through service boundaries. Remaining matrix event producers, actor attribution for bootstrap/identity/system paths, retention metadata, and SQL query pushdown remain planned work. | `apps/api/src/taroai/audit/`, `apps/api/src/taroai/app.py`, `apps/api/src/taroai/agent/runtime.py`, `apps/api/src/taroai/onboarding/bootstrap.py`, `apps/api/src/taroai/identity/`, `apps/api/src/taroai/store.py`, `apps/api/src/taroai/db/repository.py`, `tests/api/test_audit.py`, `tests/api/test_app.py`, `tests/api/test_agent_runtime.py`, `tests/api/test_tool_gateway.py`, `tests/api/test_db_repository.py`, `tests/api/test_backend_architecture_contract.py` |
| Persistence | SQL migration exists with runtime state table, and a `taroai/db` package provides Pydantic database config, migration runner, and a SQLite-compatible SQL control-plane repository for run/event/status/artifact/approval/meter/audit/runtime-state persistence. The FastAPI app and agent worker runner can select this repository through Pydantic settings. Production PostgreSQL adapter rollout, connection pooling, and migrations tooling remain implementation work. | `apps/api/migrations/001_initial.sql`, `apps/api/src/taroai/db/`, `apps/api/src/taroai/app.py`, `apps/api/src/taroai/workers/runner.py`, `tests/api/test_db_repository.py`, `tests/api/test_migration_contract.py`, `tests/api/test_app.py`, `tests/api/test_worker_runner.py` |
| Lifecycle governance | Pydantic lifecycle policy, data category, deletion behavior, legal hold, legal-hold scope, data export manifest/bundle, backup manifest, data residency report, and tenant offboarding plan models exist with in-memory and SQLite-compatible SQL stores. SQL migration tables persist lifecycle policies, legal holds, and offboarding plans. Tenant default policies and workspace overrides resolve through the lifecycle store. Cleanup worker wiring can select the lifecycle store through Pydantic Settings and storage cleanup checks active legal holds before deletion. FastAPI lifecycle policy upsert/read/effective-read, storage cleanup preview, storage-object export manifest/bundle, backup manifest, data residency report, tenant offboarding create/get/approve/export-bundle/delete, and legal-hold create/list/release endpoints exist behind `lifecycle.read`/`lifecycle.manage`, with offboarding deletion now covering storage objects, memory, and knowledge metadata/chunks. Audit events omit raw legal-hold reason text, export item details, backup component details, full residency check details, raw offboarding reason text, deleted storage object IDs, deleted memory record IDs, deleted knowledge document IDs, and legal-hold IDs. Non-storage cleanup categories beyond memory, tenant-wide asynchronous export orchestration, broader offboarding deletion orchestration, backup execution, restore drills, and physical residency enforcement remain planned work. | `apps/api/src/taroai/lifecycle/`, `apps/api/src/taroai/app.py`, `apps/api/src/taroai/storage/lifecycle.py`, `apps/api/src/taroai/workers/runner.py`, `apps/api/migrations/001_initial.sql`, `tests/api/test_lifecycle_policy.py`, `tests/api/test_lifecycle_export.py`, `tests/api/test_lifecycle_backup.py`, `tests/api/test_data_residency_config.py`, `tests/api/test_tenant_offboarding.py`, `tests/api/test_lifecycle_api.py`, `tests/api/test_storage_lifecycle.py`, `tests/api/test_worker_runner.py` |
| Workers and queue | Pydantic worker job contracts exist for run execution, billing aggregation, and cleanup, with queue claim/ack/fail/reject lifecycle tests, retry/dead-letter policy, Redis-backed queue adapter boundary, configurable API enqueue mode for run execution, worker job lifecycle audit events with actor attribution, an agent worker runner/entrypoint that injects `AuditService`, selects the configured control-plane store backend, and registers default runtime tool handlers, plus a cleanup worker runner that wires storage lifecycle cleanup through Settings. Worker deployment manifests and live Redis integration verification remain planned work. | `apps/api/src/taroai/workers/`, `tests/api/test_worker_contract.py`, `tests/api/test_worker_runner.py`, `tests/api/test_storage_lifecycle.py`, `tests/api/test_app.py` |
| Frontend | No frontend implementation is present yet. This is intentional for the current milestone; frontend is deferred to a final user-managed phase while contracts are documented. | `find apps -maxdepth 2 -type d`, `review-decisions.md` |
| Missing packages | Dedicated `connectors` and `webhooks` packages remain planned but not implemented packages. | `find apps/api/src/taroai -maxdepth 3 -type f` |

## Source-Backed Terminology

| Term | Source-Backed Meaning | How Plans Should Use It |
| --- | --- | --- |
| LangGraph | LangGraph is an orchestration runtime focused on durable execution, streaming, human-in-the-loop, and persistence. Checkpointers persist graph state; stores persist application-defined long-term data. | Treat as a candidate runtime adapter. Current code has `agent/graph.py` as a seam, not a verified LangGraph implementation. |
| LangChain agents | LangChain describes an agent as a model calling tools in a loop until a task is complete; its harness can include tools, middleware, guardrails, planning/delegation, and human-in-the-loop steering. | Use for agent loop vocabulary. Do not imply the current runtime already uses LangChain. |
| LlamaIndex / RAG | LlamaIndex RAG docs describe loading documents/nodes, indexing, storing, retrieving, querying, and evaluating. Retrievers fetch relevant context for a user query or chat message. | Treat as an optional knowledge/RAG adapter. Current code has a native `knowledge` package, not a verified LlamaIndex integration. |
| MCP tools | MCP tool servers expose tools that models can invoke; each tool has a unique name and metadata including input schema. | Say `MCP-compatible` or `MCP-style` until actual MCP client/server code exists. |
| E2B | E2B provides isolated cloud sandboxes where agents can run code, process data, and use tools; SDK docs describe Linux access, files/directories, commands, isolated code, and internet access. | Treat as a candidate sandbox provider behind `SandboxAdapter`, not as a committed or security-approved provider. |
| OpenAI-compatible Model Gateway | For Taroai planning, this means an internal gateway boundary with OpenAI-style request/response concepts such as model, messages or input, tools, tool choice, streaming, and usage. | Product flow calls this gateway. Direct OpenAI, OpenAI-compatible endpoints, or LiteLLM can be provider adapters behind the gateway after review. |
| Pydantic Settings | Pydantic Settings loads config classes from environment variables or secrets files; `BaseSettings` allows values to be overridden by environment variables and can load env files. | Current code aligns with this pattern. Keep backend settings and management objects as Pydantic models. |
| FastAPI exception handlers | FastAPI supports global custom exception handlers that return JSON responses, and warns against leaking internal validation details. | Current `ApiExceptionManager` is a valid direction. Plans should require new business exceptions to register manager rules, not route-level try/except blocks. |
| PostgreSQL RLS | PostgreSQL row security policies restrict which rows can be selected/inserted/updated/deleted; if row security is enabled and no policy exists, default deny applies. | Use as defense-in-depth for tenant isolation, not as the only authorization layer. |
| Redis TTL | Redis `EXPIRE` sets a timeout after which a key is automatically deleted. | Appropriate for short-term run/session memory; long-term memory needs durable storage and review policy. |
| pgvector | pgvector is an open-source Postgres extension for vector similarity search, supporting exact/approximate nearest neighbor search and multiple distance metrics. | Candidate durable vector backend. Do not claim it is chosen until Q-003 is answered. |
| Next.js App Router | Next.js App Router uses file-system based routing through folders/files and supports layouts/pages and route handlers in the `app` directory. | Candidate future frontend scaffold only. No frontend code should be created in the current milestone; preserve CREAO-compatible chat structure when the final frontend phase is approved. |
| OpenTelemetry | OTel is a vendor-neutral observability framework. Traces are made of spans; spans represent units of work with timestamps, attributes, events, links, and status. | Plans can reference the current OTLP HTTP export boundary. Do not claim full instrumentation or live collector deployment exists yet. |
| Self-evolving | Not a single standard framework term in the current plan set. For Taroai it means evaluation-generated improvement candidates, human/admin review, versioned publication, and rollback. | Always define as controlled change management. Do not imply autonomous production mutation. |

## Implementation Wording Rules

- Write `candidate provider` when discussing E2B, LiteLLM, pgvector, or LlamaIndex before a review decision exists.
- Write `adapter seam` when current code has only config or package boundaries.
- Write `MCP-compatible manifest` unless an actual MCP protocol implementation is added.
- Write `OTel-compatible trace model/export boundary` until full instrumentation and live collector deployment are implemented.
- Write `in-memory foundation` only for services without durable or external-adapter persistence; memory now has Redis-backed short-term put/get/list/delete/tenant-delete and SQLite-compatible long-term persistence plus tenant redaction foundations.
- Write `OpenAI-compatible Model Gateway` for model flow.
- Use `tests-only fixture adapter` only in test sections, fixtures, or contract-verification sections.
- Do not describe planned packages as implemented.
- Do not treat vendor docs as a security approval; sandbox, model, vector, and connector providers still need security review.

## Open Decisions Affected

These open questions should not be marked answered without explicit review:

- Q-002: first sandbox provider.
- Q-003: first vector backend.
- Q-004: first model gateway strategy.
- Q-006: answered; frontend implementation is deferred to the final user-managed phase.
- Q-008: API versioning timing.

## Sources Checked

- LangGraph overview: https://docs.langchain.com/oss/python/langgraph/overview
- LangGraph persistence: https://docs.langchain.com/oss/python/langgraph/persistence
- LangGraph checkpointers: https://docs.langchain.com/oss/python/langgraph/checkpointers
- LangChain agents: https://docs.langchain.com/oss/python/langchain/agents
- LlamaIndex framework docs: https://developers.llamaindex.ai/python/framework/
- LlamaIndex RAG concepts: https://developers.llamaindex.ai/python/framework/understanding/rag/
- LlamaIndex agents: https://developers.llamaindex.ai/python/framework/use_cases/agents/
- MCP tools specification: https://modelcontextprotocol.io/specification/2025-06-18/server/tools
- E2B docs: https://e2b.dev/docs
- E2B sandbox docs: https://e2b.dev/docs/sdk-reference/code-interpreter-python-sdk/v1.2.1/sandbox
- Pydantic Settings: https://pydantic.dev/docs/validation/latest/concepts/pydantic_settings/
- Pydantic BaseSettings API: https://pydantic.dev/docs/validation/latest/api/pydantic_settings/
- FastAPI error handling: https://fastapi.tiangolo.com/tutorial/handling-errors/
- PostgreSQL row security policies: https://www.postgresql.org/docs/current/ddl-rowsecurity.html
- Redis EXPIRE: https://redis.io/docs/latest/commands/expire/
- pgvector: https://github.com/pgvector/pgvector
- Next.js App Router: https://nextjs.org/docs/app
- Next.js layouts and pages: https://nextjs.org/docs/app/getting-started/layouts-and-pages
- OpenTelemetry docs: https://opentelemetry.io/docs/
- OpenTelemetry traces: https://opentelemetry.io/docs/concepts/signals/traces/
- OpenAI Responses API reference: https://platform.openai.com/docs/api-reference/responses
- OpenAI Chat Completions API reference: https://platform.openai.com/docs/api-reference/chat
